{
  "version": 1,
  "cells": [
    {
      "kind": 1,
      "language": "markdown",
      "value": "# uvm_sequence_item 详解\n\n## 1. 什么是 uvm_sequence_item\n\n它是 UVM 中所有**激励数据包 (transaction)** 的基类，直接继承自 `uvm_object`。无论你想把什么数据从一个组件传到另一个组件（比如 AXI 读写请求、以太网帧、UART 数据包），通常都从 `uvm_sequence_item` 派生。\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n        `uvm_field_int(is_write, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n## 2. 继承关系\n\n```\nuvm_object\n  └── uvm_transaction       (加了 timing 记录)\n        └── uvm_sequence_item  (加了 sequence 相关接口)\n```\n\n- **uvm_object** → 提供 `copy`、`clone`、`compare`、`print`、`pack` 等基础方法。\n- **uvm_transaction** → 在上层加了 `get_begin_time()`、`get_end_time()` 等时间戳。\n- **uvm_sequence_item** → 再加 `set_item_context()`、`get_sequence_id()` 等，让 transaction 和 sequence/sequencer 联动。\n\n## 3. 核心特性\n\n| 特性 | 说明 |\n|------|------|\n| **随机化** | 用 `rand` 声明字段，在 sequence 里通过 `randomize()` 约束生成 |\n| **field automation** | `uvm_field_*` 宏自动实现 `copy/compare/print/pack` |\n| **sequencer 绑定** | 每个 item 知道自己属于哪个 sequence，通过 `get_sequencer()` 获取 |\n| **生命周期** | 由 sequence 创建 → sequencer 发送 → driver 消费 → 自动回收 |\n\n## 4. 典型流转路径\n\n```\nsequence 创建 item\n  → start_item(item)       // 向 sequencer 申请发送许可\n  → item.randomize()       // 随机化数据\n  → finish_item(item)      // 发送给 driver\n  → driver 消费 item\n  → seq_item_port.get() / item_done()\n```\n\n## 5. 与 uvm_sequence 的关系\n\n- **sequence_item** 是\"信\"（数据载体），**sequence** 是\"寄信的人\"（控制发送节奏）。\n- sequence 里通过 `uvm_do(item)` 宏完成 item 的创建、随机化、发送全过程。\n- 一个 sequence 可以发送多个不同类型的 sequence_item。\n\n---\n\n**简单总结：** `uvm_sequence_item` = 可随机化、可打印、可比较、可打包的数据包，是 UVM 激励传输的最小单元。它和 `uvm_driver`、`uvm_sequencer`、`uvm_sequence` 一起构成了 UVM 激励发送的四大核心角色。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# set_type_override_by_type 详解\n\n## 一句话总结\n\n> **`set_type_override_by_type` 是\"按类型全局替换\"——把某个基类的所有实例，全部替换成子类。**\n\n---\n\n## 基本概念\n\nUVM 的 factory override 机制允许你在**不修改原始代码**的情况下，用子类替换基类。`set_type_override_by_type` 是最常用的方式之一。\n\n## 函数签名\n\n```systemverilog\nstatic function void set_type_override_by_type(\n    uvm_object_wrapper original_type,   // 要被替换的基类\n    uvm_object_wrapper override_type,   // 用来替换的子类\n    bit replace = 1                     // 1=强制覆盖已有override\n);\n```\n\n## 语法（两种写法）\n\n### 写法 1：传 type_id（推荐）\n\n```systemverilog\nbase_transaction::type_id::set_type_override_by_type(\n    extended_transaction::get_type()\n);\n```\n\n### 写法 2：顶层调用\n\n```systemverilog\nuvm_factory f = uvm_factory::get();\nf.set_type_override_by_type(\n    base_transaction::get_type(),\n    extended_transaction::get_type()\n);\n```\n\n## 实际例子\n\n```systemverilog\nclass base_seq_item extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n\n    `uvm_object_utils(base_seq_item)\n\n    function new(string name = \"base_seq_item\");\n        super.new(name);\n    endfunction\nendclass\n\nclass extended_seq_item extends base_seq_item;\n    rand bit [ 3:0] burst_length;   // 新增字段\n    rand bit [ 1:0] burst_type;\n\n    `uvm_object_utils(extended_seq_item)\n\n    function new(string name = \"extended_seq_item\");\n        super.new(name);\n    endfunction\nendclass\n\n// 在 test 的 build_phase 中：\nclass my_test extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        // 关键：把 base_seq_item 替换成 extended_seq_item\n        set_type_override_by_type(\n            base_seq_item::get_type(),\n            extended_seq_item::get_type()\n        );\n    endfunction\nendclass\n```\n\n## 效果\n\n执行 override 之后，**所有**通过 `type_id::create()` 创建 `base_seq_item` 的地方，实际创建出来的都是 `extended_seq_item` 对象：\n\n```systemverilog\n// 这行代码看起来创建的是 base_seq_item\nbase_seq_item item;\nitem = base_seq_item::type_id::create(\"item\");\n\n// 但实际上 item 是 extended_seq_item 类型！\n// item.burst_length 和 item.burst_type 也可以用！\n```\n\n## 核心特点\n\n| 特点 | 说明 |\n|------|------|\n| **作用范围** | 全局，对所有后续 `create()` 生效 |\n| **匹配方式** | 精确类型匹配（只看类名，不看实例路径） |\n| **时机** | 必须在 `create()` 之前调用（通常在 `build_phase` 中） |\n| **replace 参数** | 默认 1，会覆盖已有的 override |\n\n## 一句话总结\n\n`set_type_override_by_type` = 告诉 factory：\"以后凡是创建 A 类型的对象，全部给我偷偷换成 B 类型。\""
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# set_inst_override_by_type 详解\n\n## 一句话总结\n\n> **`set_inst_override_by_type` 是\"按实例路径精确替换\"——只替换指定层次路径上的那个对象，其他地方不受影响。**\n\n---\n\n## 基本概念\n\n和 `set_type_override_by_type` 的\"全局替换\"不同，`set_inst_override_by_type` 通过**实例路径（inst path）** 精确指定要替换哪个对象。\n\n## 函数签名\n\n```systemverilog\nstatic function void set_inst_override_by_type(\n    string relative_inst_path,           // 目标实例的相对路径\n    uvm_object_wrapper original_type,    // 要被替换的基类\n    uvm_object_wrapper override_type     // 用来替换的子类\n);\n```\n\n## 语法\n\n```systemverilog\n// 在 test 的 build_phase 中：\nset_inst_override_by_type(\n    \"env.agent.driver\",           // 目标路径\n    base_driver::get_type(),      // 原始类型\n    extended_driver::get_type()   // 替换类型\n);\n```\n\n## 实际例子\n\n```systemverilog\nclass base_driver extends uvm_driver #(base_seq_item);\n    `uvm_component_utils(base_driver)\n\n    function new(string name, uvm_component parent);\n        super.new(name, parent);\n    endfunction\n\n    virtual task run_phase(uvm_phase phase);\n        $display(\"base_driver running\");\n    endtask\nendclass\n\nclass extended_driver extends base_driver;\n    `uvm_component_utils(extended_driver)\n\n    function new(string name, uvm_component parent);\n        super.new(name, parent);\n    endfunction\n\n    virtual task run_phase(uvm_phase phase);\n        $display(\"extended_driver running with extra checks\");\n    endtask\nendclass\n\n// 在 test 中：\nclass my_test extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        // 只替换 env.agent1.driver，不影响 agent2.driver\n        set_inst_override_by_type(\n            \"env.agent1.driver\",\n            base_driver::get_type(),\n            extended_driver::get_type()\n        );\n    endfunction\nendclass\n```\n\n## 效果\n\n```\ntop\n└── env\n    ├── agent1\n    │   └── driver    ← 被替换成 extended_driver ✅\n    └── agent2\n        └── driver    ← 仍然是 base_driver ❌\n```\n\n只有 `env.agent1.driver` 被替换，`env.agent2.driver` 不受影响。\n\n## 核心特点\n\n| 特点 | 说明 |\n|------|------|\n| **作用范围** | 仅限指定路径的那个实例 |\n| **匹配方式** | 路径 + 类型双重匹配 |\n| **路径规则** | 相对路径，从调用者所在层次开始 |\n| **粒度** | 比 type override 更精细 |\n\n## 路径的写法\n\n```systemverilog\n// 假设在 test 中调用：\nset_inst_override_by_type(\"env.agent.monitor\", ...);\n// 实际路径：top.env.agent.monitor\n\nset_inst_override_by_type(\"agent.driver\", ...);\n// 实际路径：top.env.agent.driver（test 是 env 的父级）\n```\n\n## 一句话总结\n\n`set_inst_override_by_type` = 告诉 factory：\"只把 `env.agent1.driver` 这个位置的对象换成 B 类型，其他地方别动。\""
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# set_type_override vs set_inst_override 对比\n\n## 一张表看懂\n\n| 维度 | set_type_override_by_type | set_inst_override_by_type |\n|------|---------------------------|---------------------------|\n| **替换范围** | 全局，所有该类型实例 | 仅指定路径的那一个实例 |\n| **匹配条件** | 只按类型匹配 | 类型 + 实例路径双重匹配 |\n| **粒度** | 粗粒度 | 细粒度 |\n| **参数** | `(original, override, replace)` | `(path, original, override)` |\n| **典型场景** | 全环境统一升级 transaction | 只给某个 agent 换 driver |\n| **调用位置** | test 的 build_phase | test 的 build_phase |\n\n## 形象比喻\n\n| 方法 | 比喻 |\n|------|------|\n| `set_type_override_by_type` | 全国通知：所有\"警察\"制服换成\"特警\"制服 |\n| `set_inst_override_by_type` | 单独通知：只给\"北京市朝阳区派出所\"的警察换制服 |\n\n## 什么时候用哪个？\n\n| 场景 | 推荐 |\n|------|------|\n| 想全局替换所有 transaction | `set_type_override` |\n| 只想替换某个特定 agent 的 driver | `set_inst_override` |\n| 想替换某个 monitor | `set_inst_override` |\n| 想给所有 scoreboard 换实现 | `set_type_override` |\n\n## 注意事项\n\n- **时机**：必须在 `build_phase` 中调用，在 `create()` 之前生效\n- **顺序**：`set_inst_override` 优先级高于 `set_type_override`\n- **继承**：子类可以 override 父类，但父类不能 override 子类\n- **路径错误**：如果 inst path 写错了，override 静默失效，不会报错\n\n## 一句话总结\n\n> `set_type_override` 是\"全局广播\"，`set_inst_override` 是\"定点通知\"。前者管全部，后者管一个。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# uvm_transaction 详解\n\n## 一句话总结\n\n> **`uvm_transaction` 是 `uvm_sequence_item` 的父类，在 `uvm_object` 的基础上加了时间戳和记录功能。**\n\n---\n\n## 继承位置\n\n```\nuvm_object\n  └── uvm_transaction       ← 加了 timing 记录\n        └── uvm_sequence_item  ← 加了 sequence 接口\n```\n\n## 相比 uvm_object 多了什么\n\n### 1. 时间戳\n\n```systemverilog\nclass uvm_transaction extends uvm_object;\n    // 记录事务的生命周期时间\n    function uvm_time get_begin_time();    // 事务开始时间\n    function uvm_time get_end_time();      // 事务结束时间\n    function void    set_begin_time(uvm_time t);\n    function void    set_end_time(uvm_time t);\nendclass\n```\n\n### 2. 事务记录 (transaction recording)\n\n```systemverilog\n// 开启/关闭事务记录（用于波形/日志）\nfunction void enable_recording();\nfunction void disable_recording();\nfunction bit  is_recording_enabled();\n\n// 记录事务到数据库\nfunction void record(uvm_recorder recorder);\nfunction void do_record(uvm_recorder recorder);  // 用户可重写\n```\n\n### 3. accept_tr / begin_tr / end_tr\n\n```systemverilog\n// 在 driver 中典型用法：\nclass my_driver extends uvm_driver #(my_transaction);\n    task run_phase(uvm_phase phase);\n        forever begin\n            seq_item_port.get_next_item(req);\n\n            // 记录事务开始\n            req.set_begin_time($realtime);\n            accept_tr(req, $realtime);        // 接受事务\n            begin_tr(req, 0);                 // 开始记录\n\n            // 驱动事务...\n            drive(req);\n\n            // 记录事务结束\n            end_tr(req, 0);\n            req.set_end_time($realtime);\n\n            seq_item_port.item_done();\n        end\n    endtask\nendclass\n```\n\n## 核心方法一览\n\n| 方法 | 说明 |\n|------|------|\n| `get_begin_time()` | 获取事务开始时间 |\n| `get_end_time()` | 获取事务结束时间 |\n| `accept_tr()` | 标记事务被 driver 接受 |\n| `begin_tr()` | 开始事务记录（可显示在波形中） |\n| `end_tr()` | 结束事务记录 |\n| `enable_recording()` | 开启事务记录到数据库 |\n| `do_record()` | 用户可重写，定义记录哪些字段 |\n\n## 什么时候直接用 uvm_transaction\n\n虽然 UVM 常用 `uvm_sequence_item`（因为它有 sequence 接口），但如果你不需要 sequence/sequencer 机制，可以直接用 `uvm_transaction`：\n\n```systemverilog\nclass my_simple_transaction extends uvm_transaction;\n    rand bit [31:0] data;\n\n    `uvm_object_utils(my_simple_transaction)\n\n    function new(string name = \"my_simple_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n典型场景：\n- 不经过 sequencer 的简单数据包\n- 只用于记录和打印，不需要随机化\n- 自定义通信机制\n\n## 一句话总结\n\n`uvm_transaction` = `uvm_object` + 时间戳 + 事务记录，是 `uvm_sequence_item` 的\"上一个版本\"，给普通 data object 加上了生命周期追踪能力。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# rand_mode 详解\n\n## 一句话总结\n\n> **`rand_mode()` 用来动态控制某个随机字段是否参与随机化——可以随时把某个字段的随机化\"开关\"打开或关上。**\n\n---\n\n## 基本语法\n\n```systemverilog\n// 关闭某个字段的随机化\nitem.addr.rand_mode(0);\n\n// 开启某个字段的随机化\nitem.addr.rand_mode(1);\n\n// 检查某个字段的随机化状态\nbit status = item.addr.rand_mode();\n\n// 关闭整个对象的所有随机字段\nitem.rand_mode(0);\n\n// 开启整个对象的所有随机字段\nitem.rand_mode(1);\n```\n\n## 返回值\n\n- 返回**设置前的状态**（0 或 1），方便你保存和恢复\n\n## 实际例子\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n        `uvm_field_int(is_write, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 在 sequence 中使用：\nclass my_sequence extends uvm_sequence #(my_transaction);\n    task body();\n        my_transaction item;\n\n        // 场景1：只随机化 addr，data 和 is_write 固定\n        item = my_transaction::type_id::create(\"item\");\n        item.addr.rand_mode(1);      // addr 参与随机化\n        item.data.rand_mode(0);      // data 不参与随机化\n        item.is_write.rand_mode(0);  // is_write 不参与随机化\n        item.data = 32'hDEAD_BEEF;   // 手动赋值\n        item.is_write = 1;\n        item.randomize();            // 只有 addr 会被随机化\n\n        // 场景2：先保存状态，后面恢复\n        bit saved_addr = item.addr.rand_mode();\n        bit saved_data = item.data.rand_mode();\n\n        item.addr.rand_mode(0);\n        item.data.rand_mode(0);\n        // ... 做一些事 ...\n\n        // 恢复\n        item.addr.rand_mode(saved_addr);\n        item.data.rand_mode(saved_data);\n\n        // 场景3：关闭整个对象，全手动赋值\n        item.rand_mode(0);\n        item.addr = 32'h1000_0000;\n        item.data = 32'h1234_5678;\n        item.is_write = 0;\n        // 此时 randomize() 不会有任何效果\n    endtask\nendclass\n```\n\n## 与 constraint_mode 的区别\n\n| 特性 | `rand_mode` | `constraint_mode` |\n|------|-------------|-------------------|\n| 控制对象 | 字段（变量） | 约束块 |\n| 作用 | 字段是否参与随机化 | 约束是否生效 |\n| 关闭后 | 字段保持当前值不变 | 约束不检查，字段可能取任意值 |\n| 语法 | `obj.field.rand_mode(0)` | `obj.constraint_name.constraint_mode(0)` |\n\n## 典型应用场景\n\n| 场景 | 用法 |\n|------|------|\n| 固定某些字段，只随机化部分字段 | 关闭不需要随机化的字段 |\n| 定向测试（directed test） | 关闭所有随机化，手动赋值 |\n| 调试 | 关闭随机化，用固定值复现问题 |\n| 条件随机化 | 根据条件动态开关某些字段 |\n\n## 注意事项\n\n- `rand_mode(0)` 只影响 `randomize()`，不影响手动赋值\n- 必须在 `randomize()` 之前调用才有效\n- 对象的 `rand_mode(0)` 会关闭所有字段，字段级 `rand_mode(0)` 只关闭单个字段\n- 非 `rand` 字段调用 `rand_mode()` 会编译报错\n\n## 一句话总结\n\n`rand_mode` = 随机化的\"开关\"，可以精确控制每个字段是否参与随机化，实现灵活的激励生成。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 事务描述符 (Transaction Descriptor)\n\n## 一句话总结\n\n> **事务描述符就是给 transaction 添加一套描述信息（如名字、类型、ID、状态等），让它能自我描述\"我是谁、我从哪来、我什么状态\"。**\n\n---\n\n## 为什么需要事务描述符\n\n一个简单的 transaction 只有数据字段（addr、data 等），但实际工程中，你还需要知道：\n\n- 这个事务叫什么名字？\n- 它是读还是写？\n- 它的 ID 是多少？\n- 它属于哪个 sequence？\n- 它成功还是失败了？\n\n这些就是**事务描述符**要解决的问题。\n\n## 内置的序列号机制\n\n`uvm_sequence_item` 自带一个 `transaction_id`：\n\n```systemverilog\nclass uvm_sequence_item extends uvm_transaction;\n    int unsigned transaction_id;\n    function int unsigned get_transaction_id();\n    function void set_transaction_id(int unsigned id);\nendclass\n```\n\n在 sequence 中发送 item 时，sequencer 会自动分配递增的 ID。\n\n## 自定义事务描述符\n\n### 方式 1：在 transaction 中直接加字段\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    // 数据字段\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n\n    // 事务描述符\n    rand bit        is_write;       // 操作类型\n    rand bit [ 3:0] burst_len;      // 突发长度\n    rand bit [ 1:0] burst_type;     // 突发类型\n    bit [ 7:0]      status;         // 状态（非随机）\n    string          description;    // 描述字符串\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n        `uvm_field_int(is_write, UVM_ALL_ON)\n        `uvm_field_int(burst_len, UVM_ALL_ON)\n        `uvm_field_int(burst_type, UVM_ALL_ON)\n        `uvm_field_int(status, UVM_ALL_ON)\n        `uvm_field_string(description, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n        status = 0;\n        description = \"\";\n    endfunction\nendclass\n```\n\n### 方式 2：使用枚举定义描述符\n\n```systemverilog\ntypedef enum {\n    IDLE, READ, WRITE, ATOMIC\n} operation_type_e;\n\ntypedef enum {\n    OK, ERROR, TIMEOUT, RETRY\n} response_status_e;\n\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0]      addr;\n    rand bit [31:0]      data;\n\n    // 使用枚举做描述符\n    rand operation_type_e op_type;\n    response_status_e     resp_status;\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n        `uvm_field_enum(operation_type_e, op_type, UVM_ALL_ON)\n        `uvm_field_enum(response_status_e, resp_status, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n        resp_status = OK;\n    endfunction\nendclass\n```\n\n### 方式 3：在 do_record 中记录描述符\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    function void do_record(uvm_recorder recorder);\n        super.do_record(recorder);\n\n        // 将描述符信息记录到波形数据库\n        `uvm_record_field(\"addr\", addr);\n        `uvm_record_field(\"data\", data);\n        `uvm_record_field(\"is_write\", is_write);\n        `uvm_record_field(\"transaction_id\", get_transaction_id());\n    endfunction\nendclass\n```\n\n### 方式 4：在 convert2string 中组合描述符\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    function string convert2string();\n        string s;\n        s = $sformatf(\"Transaction #%0d: %s addr=0x%0h data=0x%0h\",\n                      get_transaction_id(),\n                      is_write ? \"WRITE\" : \"READ\",\n                      addr, data);\n        return s;\n    endfunction\nendclass\n```\n\n## 典型描述符字段清单\n\n| 字段 | 类型 | 说明 |\n|------|------|------|\n| `transaction_id` | int | 内置自动递增 ID |\n| `op_type` | enum | 操作类型（READ/WRITE/IDLE） |\n| `burst_len` | int | 突发长度 |\n| `burst_type` | enum | 突发类型（FIXED/INCR/WRAP） |\n| `resp_status` | enum | 响应状态（OK/ERROR/TIMEOUT） |\n| `priority` | int | 优先级 |\n| `timestamp` | time | 时间戳 |\n| `description` | string | 可读描述 |\n\n## 设计原则\n\n1. **数据字段** vs **描述符**：数据字段是\"内容\"（addr/data），描述符是\"元信息\"（type/status/id）\n2. **描述符也应该是 rand**：操作类型、burst 参数等描述符也应该随机化，才能覆盖更多场景\n3. **状态字段不要 rand**：响应状态（OK/ERROR）是 driver 或 monitor 在事后填写的，不是随机生成的\n4. **用 convert2string 做日志**：实现 `convert2string()` 让每个 transaction 都有一行可读的日志\n\n## 一句话总结\n\n事务描述符 = 给 transaction 加上\"身份标签\"（ID、类型、状态），让每个事务都能自我描述，方便调试、记录和追踪。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 事务约束 (Constraints)\n\n## 一句话总结\n\n> **约束定义了事务字段的合法取值范围，是 randomize() 时必须遵守的\"规则\"。**\n\n---\n\n## 约束的分类\n\n### 1. 必须遵守的约束 (Hard Constraints)\n\n写在 `constraint` 块里的约束，`randomize()` 时必须满足，否则随机化失败。\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit [ 3:0] burst_len;\n\n    // 必须遵守的约束\n    constraint c_addr_align {\n        addr[1:0] == 0;              // 地址必须 4 字节对齐\n    }\n\n    constraint c_burst_len {\n        burst_len inside {[1:16]};    // 突发长度 1~16\n        burst_len != 0;               // 长度不能为 0\n    }\n\n    constraint c_data_range {\n        data inside {[32'h0000_0000:32'hFFFF_0000]};  // 数据范围\n    }\nendclass\n```\n\n### 2. 应该遵守的约束 (Soft Constraints)\n\n用 `soft` 关键字修饰，优先级低于 hard constraint，可以被覆盖而不报错。\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n\n    // 软约束：建议值，但可以被覆盖\n    constraint c_default {\n        soft addr == 32'h1000_0000;   // 建议起始地址\n        soft data == 32'h0;           // 建议数据为 0\n    }\nendclass\n\n// 在 sequence 中覆盖软约束：\nclass my_sequence extends uvm_sequence #(my_transaction);\n    task body();\n        my_transaction item;\n        item = my_transaction::type_id::create(\"item\");\n\n        // 内联约束覆盖软约束，不会报错\n        item.randomize() with {\n            addr == 32'h2000_0000;   // 覆盖了 soft 建议值\n        };\n    endtask\nendclass\n```\n\n## 如何添加约束\n\n### 方式 1：在类定义中写 constraint 块\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    // 约束 1：地址对齐\n    constraint c_addr_align {\n        addr[1:0] == 0;\n    }\n\n    // 约束 2：写操作时数据不能全是 0\n    constraint c_write_data {\n        if (is_write) {\n            data != 0;\n        }\n    }\n\n    // 约束 3：使用 inside 集合\n    constraint c_valid_range {\n        addr inside {[32'h0000_0000:32'h7FFF_FFFF]};  // 范围\n        is_write dist {0 := 30, 1 := 70};             // 权重分布\n    }\nendclass\n```\n\n### 方式 2：在 sequence 中用内联约束 (inline constraint)\n\n```systemverilog\nclass my_sequence extends uvm_sequence #(my_transaction);\n    task body();\n        my_transaction item;\n\n        // with 子句添加额外约束\n        item = my_transaction::type_id::create(\"item\");\n        item.randomize() with {\n            addr inside {[32'h1000_0000:32'h1000_FFFF]};\n            is_write == 1;\n            data == 32'hDEAD_BEEF;\n        };\n    endtask\nendclass\n```\n\n### 方式 3：继承子类添加约束\n\n```systemverilog\nclass extended_transaction extends my_transaction;\n    rand bit [3:0] burst_len;\n\n    // 子类新约束\n    constraint c_burst_len {\n        burst_len inside {1, 2, 4, 8, 16};  // 只允许 2 的幂次\n    }\n\n    // 子类可以覆盖父类同名约束（前提是父类约束允许 override）\n    constraint c_valid_range {\n        addr inside {[32'h8000_0000:32'hFFFF_FFFF]};  // 不同地址范围\n    }\nendclass\n```\n\n### 方式 4：用 constraint_mode 动态开关约束\n\n```systemverilog\n// 关闭某个约束块\nitem.c_addr_align.constraint_mode(0);\n\n// 开启某个约束块\nitem.c_addr_align.constraint_mode(1);\n\n// 检查约束块状态\nbit status = item.c_addr_align.constraint_mode();\n```\n\n## 约束运算符一览\n\n| 运算符 | 含义 | 示例 |\n|--------|------|------|\n| `inside` | 属于集合 | `addr inside {[0:1023], 2048}` |\n| `dist` | 权重分布 | `is_write dist {0:=30, 1:=70}` |\n| `->` | 蕴含 | `is_write -> data != 0` |\n| `if-else` | 条件 | `if (is_write) data != 0` |\n| `solve-before` | 求解顺序 | `solve is_write before data` |\n| `soft` | 软约束 | `soft addr == 0` |\n| `unique` | 唯一性 | `unique {addr, data}` |\n\n## 约束设计原则\n\n| 原则 | 说明 |\n|------|------|\n| **完备性** | 约束应覆盖所有合法场景，不能遗漏 |\n| **合法性** | 约束必须保证字段值在实际硬件中合法 |\n| **可解性** | 约束不能自相矛盾导致 randomize() 失败 |\n| **分层** | 基类约束放通用规则，子类约束放特定规则 |\n| **可关闭** | 关键约束块要命名，方便 constraint_mode(0) 关闭 |\n\n## 一句话总结\n\n约束 = transaction 的\"法律\"，hard constraint 必须遵守，soft constraint 可以建议，用 `constraint` 块定义、用 `with` 子句增强、用 `constraint_mode` 开关。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# do_* 数据处理函数详解\n\n## 一句话总结\n\n> **`do_*` 系列函数是 UVM 留给用户的数据处理钩子（hook），重写它们就能自定义 transaction 的复制、比较、打印、打包等行为。**\n\n---\n\n## do_* 函数全景图\n\n```\n用户调用（public）          内部调用（用户重写 do_*）\n─────────────────────      ──────────────────────────\nobj.copy(other)      →      do_copy(other)\nobj.clone()          →      do_copy() + create()\nobj.compare(other)   →      do_compare(other)\nobj.print()          →      do_print(printer)\nobj.pack()           →      do_pack(packer)\nobj.unpack()         →      do_unpack(packer)\nobj.record(rec)      →      do_record(rec)\nobj.convert2string() →      (直接重写，没有 do_ 前缀)\n```\n\n## 逐个详解\n\n### 1. do_copy — 复制\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    bit [ 7:0]      status;   // 非 rand 字段\n\n    function void do_copy(uvm_object rhs);\n        my_transaction t;\n        $cast(t, rhs);\n        super.do_copy(rhs);     // 先调父类\n\n        // 复制自己的字段\n        this.addr   = t.addr;\n        this.data   = t.data;\n        this.status = t.status;\n    endfunction\nendclass\n\n// 使用：\nmy_transaction t1 = new(\"t1\");\nmy_transaction t2 = new(\"t2\");\nt1.copy(t2);   // 内部调用 do_copy\n```\n\n### 2. do_compare — 比较\n\n```systemverilog\nfunction bit do_compare(uvm_object rhs, uvm_comparer comparer);\n    my_transaction t;\n    $cast(t, rhs);\n\n    return (super.do_compare(rhs, comparer) &&\n            (this.addr == t.addr) &&\n            (this.data == t.data) &&\n            (this.status == t.status));\nendfunction\n\n// 使用：\nif (t1.compare(t2))  // 内部调用 do_compare\n    $display(\"Match!\");\n```\n\n### 3. do_print — 打印\n\n```systemverilog\nfunction void do_print(uvm_printer printer);\n    super.do_print(printer);\n\n    printer.print_field_int(\"addr\",   addr,   32, UVM_HEX);\n    printer.print_field_int(\"data\",   data,   32, UVM_HEX);\n    printer.print_field_int(\"status\", status,  8, UVM_DEC);\n    printer.print_string(\"info\", \"custom transaction\");\nendfunction\n\n// 使用：\nt1.print();  // 内部调用 do_print\n// 输出：\n// addr  : 0x1000_0000\n// data  : 0xDEAD_BEEF\n// status: 0\n// info  : custom transaction\n```\n\n### 4. do_pack / do_unpack — 打包/解包\n\n```systemverilog\nfunction void do_pack(uvm_packer packer);\n    super.do_pack(packer);\n\n    `uvm_pack_int(addr);\n    `uvm_pack_int(data);\n    `uvm_pack_int(status);\nendfunction\n\nfunction void do_unpack(uvm_packer packer);\n    super.do_unpack(packer);\n\n    `uvm_unpack_int(addr);\n    `uvm_unpack_int(data);\n    `uvm_unpack_int(status);\nendfunction\n\n// 使用：\nbit vector[];\nt1.pack(vector);    // 序列化 → 内部调用 do_pack\nt2.unpack(vector);  // 反序列化 → 内部调用 do_unpack\n```\n\n### 5. do_record — 记录到波形\n\n```systemverilog\nfunction void do_record(uvm_recorder recorder);\n    super.do_record(recorder);\n\n    `uvm_record_field(\"addr\", addr);\n    `uvm_record_field(\"data\", data);\n    `uvm_record_field(\"status\", status);\nendfunction\n\n// 在 driver 中：\nbegin_tr(req, 0);   // 初始化记录\n// ... drive ...\nend_tr(req, 0);     // 调用 do_record 记录字段\n```\n\n### 6. convert2string — 转字符串\n\n```systemverilog\nfunction string convert2string();\n    return $sformatf(\"[%s] addr=0x%0h data=0x%0h status=%0d\",\n                      get_name(), addr, data, status);\nendfunction\n\n// 使用：\n`uvm_info(\"SEQ\", $sformatf(\"Sent: %s\", item.convert2string()), UVM_MEDIUM);\n```\n\n## 什么时候需要手写 do_*\n\n| 场景 | 说明 |\n|------|------|\n| 有非 rand 字段 | 状态字段需要手动 copy/compare |\n| 有指针/动态数组 | 需要深拷贝，不能简单赋值 |\n| 需要自定义格式 | 打印格式、打包顺序有特殊要求 |\n| 性能优化 | 跳过不需要的字段，加速 compare/pack |\n| 有特殊逻辑 | 复制时需要做某些转换 |\n\n## 一句话总结\n\n`do_*` = UVM 的数据处理\"钩子\"，重写它们就能自定义 copy/compare/print/pack/unpack/record 的行为，不重写就用 `uvm_field_*` 宏的默认实现。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# UVM 宏定义创建通用数据处理函数\n\n## 一句话总结\n\n> **`uvm_field_*` 宏自动生成 `do_copy`、`do_compare`、`do_print`、`do_pack`、`do_unpack`、`do_record` 的实现，不需要手写 do_* 函数。**\n\n---\n\n## 宏的工作机制\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit        is_write;\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr,     UVM_ALL_ON)   // 自动生成所有 do_*\n        `uvm_field_int(data,     UVM_ALL_ON)\n        `uvm_field_int(is_write, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n三行 `uvm_field_int` 宏，就自动生成了：\n- `do_copy` — 复制 addr, data, is_write\n- `do_compare` — 比较 addr, data, is_write\n- `do_print` — 打印 addr, data, is_write\n- `do_pack` / `do_unpack` — 打包/解包 addr, data, is_write\n- `do_record` — 记录 addr, data, is_write 到波形\n\n## 常用 uvm_field 宏清单\n\n| 宏 | 适用类型 | 示例 |\n|------|----------|------|\n| `uvm_field_int` | 整数类型 | `uvm_field_int(addr, UVM_ALL_ON)` |\n| `uvm_field_enum` | 枚举类型 | `uvm_field_enum(op_type_e, op, UVM_ALL_ON)` |\n| `uvm_field_string` | 字符串 | `uvm_field_string(name, UVM_ALL_ON)` |\n| `uvm_field_real` | 实数 | `uvm_field_real(voltage, UVM_ALL_ON)` |\n| `uvm_field_object` | uvm_object 子对象 | `uvm_field_object(sub_obj, UVM_ALL_ON)` |\n| `uvm_field_array_int` | 整数动态数组 | `uvm_field_array_int(arr, UVM_ALL_ON)` |\n| `uvm_field_queue_int` | 整数队列 | `uvm_field_queue_int(q, UVM_ALL_ON)` |\n| `uvm_field_sarray_int` | 整数静态数组 | `uvm_field_sarray_int(sarr, UVM_ALL_ON)` |\n| `uvm_field_event` | event | `uvm_field_event(evt, UVM_ALL_ON)` |\n\n## 标志位 (Flag) 详解\n\n第二个参数控制哪些 do_* 函数被生成：\n\n| Flag | 含义 | 效果 |\n|------|------|------|\n| `UVM_ALL_ON` | 全部开启 | 生成所有 do_* |\n| `UVM_DEFAULT` | 默认 | = 全部开启 |\n| `UVM_NOCOPY` | 不参与 copy | 关闭 do_copy |\n| `UVM_NOCOMPARE` | 不参与 compare | 关闭 do_compare |\n| `UVM_NOPRINT` | 不参与 print | 关闭 do_print |\n| `UVM_NOPACK` | 不参与 pack/unpack | 关闭 do_pack/do_unpack |\n| `UVM_NORECORD` | 不参与 record | 关闭 do_record |\n\n### 组合使用\n\n```systemverilog\n`uvm_object_utils_begin(my_transaction)\n    // 全部参与\n    `uvm_field_int(addr,  UVM_ALL_ON)\n\n    // 只参与 copy 和 print，不参与 compare 和 pack\n    `uvm_field_int(data,  UVM_NOCOMPARE | UVM_NOPACK)\n\n    // 只参与 print（调试用）\n    `uvm_field_int(debug, UVM_NOPRINT)\n\n    // 不参与任何操作（纯手动管理）\n    `uvm_field_int(temp,  UVM_NOPACK | UVM_NOPRINT | UVM_NOCOPY | UVM_NOCOMPARE)\n`uvm_object_utils_end\n```\n\n## 宏 vs 手写 do_* 对比\n\n| 维度 | `uvm_field_*` 宏 | 手写 `do_*` |\n|------|------------------|-------------|\n| **代码量** | 少（一行一个字段） | 多（每个 do_* 都要写） |\n| **维护性** | 好（加字段只需加一行宏） | 差（每个 do_* 都要改） |\n| **灵活性** | 受限（只能按固定格式） | 高（可以做任意逻辑） |\n| **性能** | 一般（反射开销） | 好（直接操作） |\n| **适用场景** | 90% 的日常开发 | 需要特殊逻辑时 |\n\n## 混用模式（推荐）\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    bit [ 7:0]      status;    // 非 rand 字段\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr,   UVM_ALL_ON)   // 宏自动处理\n        `uvm_field_int(data,   UVM_ALL_ON)\n        `uvm_field_int(status, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    // 但对 convert2string 有特殊需求，手写\n    function string convert2string();\n        string s;\n        s = super.convert2string();\n        return $sformatf(\"%s [status=%0d]\", s, status);\n    endfunction\n\n    // 对 do_print 有特殊需求，手写覆盖宏的默认\n    function void do_print(uvm_printer printer);\n        super.do_print(printer);\n        printer.print_string(\"note\", \"custom format\");\n    endfunction\nendclass\n```\n\n## 常见数据处理函数一览\n\n| 函数 | 功能 | 如何生成 |\n|------|------|----------|\n| `copy()` | 浅复制 | `uvm_field_*` 宏 或 手写 `do_copy` |\n| `clone()` | 深复制（创建新对象） | `create()` + `copy()` |\n| `compare()` | 逐字段比较 | `uvm_field_*` 宏 或 手写 `do_compare` |\n| `print()` | 格式化打印 | `uvm_field_*` 宏 或 手写 `do_print` |\n| `pack()` | 序列化为 bit 流 | `uvm_field_*` 宏 或 手写 `do_pack` |\n| `unpack()` | 从 bit 流反序列化 | `uvm_field_*` 宏 或 手写 `do_unpack` |\n| `record()` | 记录到波形数据库 | `uvm_field_*` 宏 或 手写 `do_record` |\n| `convert2string()` | 转为可读字符串 | 手写（没有对应的宏） |\n\n## 一句话总结\n\n`uvm_field_*` 宏 = \"一行代码搞定所有数据处理\"，用 `UVM_ALL_ON` 生成全部，用 Flag 组合精确控制，特殊需求再手写 `do_*` 覆盖。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# uvm_object_utils 宏族详解\n\n## 一句话总结\n\n> **`uvm_object_utils` 宏族是 UVM factory 注册 + field automation 的\"一键开关\"，让 transaction 能被 factory 创建、能用 `uvm_field_*` 自动生成数据处理函数。**\n\n---\n\n## 宏族分类\n\n### 1. 无 field 宏 — 只注册不自动化\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n\n    `uvm_object_utils(my_transaction)   // 只注册 factory，无 field\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n效果：\n- ✅ 可以用 `type_id::create()` 创建\n- ✅ 可以用 factory override\n- ❌ 没有自动化的 copy/compare/print/pack\n\n### 2. 有 field 宏 — 注册 + 自动化\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n\n    `uvm_object_utils_begin(my_transaction)\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n效果：\n- ✅ 可以用 `type_id::create()` 创建\n- ✅ 可以用 factory override\n- ✅ 自动生成 copy/compare/print/pack/record\n\n### 3. 参数化类的宏\n\n```systemverilog\nclass my_transaction #(int WIDTH = 32) extends uvm_sequence_item;\n    rand bit [WIDTH-1:0] addr;\n    rand bit [WIDTH-1:0] data;\n\n    `uvm_object_param_utils_begin(my_transaction #(WIDTH))\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n## 宏族对照表\n\n| 宏 | 适用场景 | 是否支持 field |\n|------|----------|:---:|\n| `uvm_object_utils(T)` | 普通类，不需要 field | ❌ |\n| `uvm_object_utils_begin(T)` | 普通类，需要 field | ✅ |\n| `uvm_object_param_utils(T)` | 参数化类，不需要 field | ❌ |\n| `uvm_object_param_utils_begin(T)` | 参数化类，需要 field | ✅ |\n| `uvm_component_utils(T)` | 普通组件，不需要 field | ❌ |\n| `uvm_component_utils_begin(T)` | 普通组件，需要 field | ✅ |\n\n## 宏展开后做了什么\n\n```systemverilog\n// `uvm_object_utils(my_transaction) 展开后大致等价于：\n\ntypedef uvm_object_registry #(my_transaction) type_id;\n\nstatic function type_id get_type();\n    return type_id::get();\nendfunction\n\nvirtual function uvm_object_wrapper get_object_type();\n    return type_id::get();\nendfunction\n```\n\n关键点：\n1. **`type_id`** — 一个静态注册器，让 factory 知道这个类的存在\n2. **`get_type()`** — 返回 type_id，用于 factory override\n3. **`get_object_type()`** — 虚函数，运行时获取真实类型\n\n## 为什么必须用这些宏\n\n不用宏的后果：\n\n```systemverilog\n// ❌ 没有注册 macro\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\nendclass\n\n// 无法用 factory 创建！\nmy_transaction item;\nitem = my_transaction::type_id::create(\"item\");  // 编译报错！\n\n// 只能手动 new\nitem = new(\"item\");\n\n// 而且 factory override 失效！\nset_type_override_by_type(...);  // 对这个类无效！\n```\n\n## 一句话总结\n\n`uvm_object_utils` = factory 注册 + type_id 创建 + field automation，是 UVM 中每个 transaction 必须写的\"身份证\"。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# uvm_field_* 通过 Flag 设置数据处理函数\n\n## 一句话总结\n\n> **`uvm_field_*` 的第二个参数（Flag）精确控制哪些数据处理函数被自动生成，实现\"按需生成\"而不是\"全量生成\"。**\n\n---\n\n## Flag 与数据处理函数的对应关系\n\n```\nFlag              →  自动生成的函数\n──────────────────────────────────────────\nUVM_ALL_ON        →  copy + compare + print + pack + unpack + record\nUVM_DEFAULT       →  同上（全部）\nUVM_NOCOPY        →  关闭 copy(clone)\nUVM_NOCOMPARE     →  关闭 compare\nUVM_NOPRINT       →  关闭 print\nUVM_NOPACK        →  关闭 pack + unpack\nUVM_NORECORD      →  关闭 record\n```\n\n## 实战：不同 Flag 组合的效果\n\n```systemverilog\nclass my_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;      // 核心数据\n    rand bit [31:0] data;      // 核心数据\n    bit [ 7:0]      checksum;  // 计算字段，不应参与 compare\n    string          debug_msg; // 调试信息，不应参与 pack\n    int             temp;      // 临时变量，什么都不参与\n\n    `uvm_object_utils_begin(my_transaction)\n        // 全部参与（默认）\n        `uvm_field_int(addr, UVM_ALL_ON)\n\n        // 全部参与\n        `uvm_field_int(data, UVM_ALL_ON)\n\n        // checksum 是计算出来的，比较时跳过\n        `uvm_field_int(checksum, UVM_NOCOMPARE)\n\n        // debug_msg 不需要打包传输\n        `uvm_field_string(debug_msg, UVM_NOPACK)\n\n        // temp 是临时的，什么都不参与\n        `uvm_field_int(temp, UVM_NOPACK | UVM_NOPRINT | UVM_NOCOPY | UVM_NOCOMPARE)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n## 常见 Flag 组合及使用场景\n\n| 组合 | 场景 | 示例 |\n|------|------|------|\n| `UVM_ALL_ON` | 普通数据字段 | addr, data |\n| `UVM_NOCOMPARE` | 计算字段（CRC、checksum） | checksum, parity |\n| `UVM_NOPACK` | 调试/日志字段 | debug_msg, timestamp |\n| `UVM_NOPRINT` | 敏感数据（密码） | password, key |\n| `UVM_NOPACK \\| UVM_NOPRINT` | 内部临时变量 | temp, scratch |\n| `UVM_NOCOPY \\| UVM_NOCOMPARE` | 运行时状态字段 | status, phase |\n| `UVM_NOPACK \\| UVM_NOCOMPARE` | 只用于打印的字段 | description, info |\n\n## Flag 如何影响 do_* 函数\n\n以 `UVM_NOCOMPARE` 为例，宏展开后生成的 `do_compare` 中会跳过该字段：\n\n```systemverilog\n// 宏展开后，do_compare 大致等价于：\nfunction bit do_compare(uvm_object rhs, uvm_comparer comparer);\n    my_transaction t;\n    $cast(t, rhs);\n\n    do_compare = 1;\n    do_compare &= (this.addr == t.addr);      // UVM_ALL_ON → 参与\n    do_compare &= (this.data == t.data);      // UVM_ALL_ON → 参与\n    // checksum 使用 UVM_NOCOMPARE → 不参与比较！\nendfunction\n```\n\n## 运行时动态修改 Flag 的局限性\n\nFlag 是**编译时**决定的，运行时无法修改。但可以用 `constraint_mode` / `rand_mode` 动态控制：\n\n```systemverilog\n// ❌ 运行时无法改 Flag\nitem.addr.uvm_field_flag = UVM_NOCOMPARE;  // 不存在这种语法\n\n// ✅ 用 constraint_mode 控制约束\nitem.c_addr_align.constraint_mode(0);\n\n// ✅ 用 rand_mode 控制随机化\nitem.addr.rand_mode(0);\n```\n\n## 一句话总结\n\nFlag = 数据处理函数的\"开关面板\"，`UVM_ALL_ON` 全开，组合 Flag 按需关闭，编译时决定，运行时不可改。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# UVM 数据处理函数的使用方法\n\n## 一句话总结\n\n> **数据处理函数是 UVM 中操作 transaction 的\"标准工具箱\"，覆盖了复制、比较、打印、打包、记录等全部生命周期。**\n\n---\n\n## 使用全景图\n\n```\n创建 → 随机化 → 打印(调试) → copy(备份) → pack(传输) → compare(检查) → record(波形)\n```\n\n## 1. copy / clone — 复制\n\n### 使用场景\n\n```systemverilog\nclass my_sequence extends uvm_sequence #(my_transaction);\n    task body();\n        my_transaction item, backup;\n\n        item = my_transaction::type_id::create(\"item\");\n        item.randomize();\n\n        // 场景1：备份原始 transaction\n        backup = my_transaction::type_id::create(\"backup\");\n        backup.copy(item);\n\n        // 对 item 做修改...\n        item.addr = item.addr + 4;\n\n        // 场景2：clone 创建独立副本\n        my_transaction cloned;\n        $cast(cloned, item.clone());  // clone = create + copy\n\n        // 场景3：在 scoreboard 中保存预期值\n        expected_queue.push_back(cloned);\n    endtask\nendclass\n```\n\n### copy vs clone\n\n| 操作 | 说明 | 代码 |\n|------|------|------|\n| `copy()` | 浅复制，目标对象必须已存在 | `t2.copy(t1)` |\n| `clone()` | 深复制，自动 new 新对象 | `t2 = t1.clone()` |\n\n## 2. compare — 比较\n\n### 使用场景\n\n```systemverilog\n// 场景1：scoreboard 中比较预期值和实际值\nclass my_scoreboard extends uvm_scoreboard;\n    function void compare_transactions(my_transaction expected,\n                                        my_transaction actual);\n        if (!expected.compare(actual)) begin\n            `uvm_error(\"SBD\", $sformatf(\n                \"Mismatch!\\nExpected: %s\\nActual: %s\",\n                expected.convert2string(),\n                actual.convert2string()\n            ))\n        end else begin\n            `uvm_info(\"SBD\", \"Match!\", UVM_HIGH)\n        end\n    endfunction\nendclass\n\n// 场景2：使用 uvm_comparer 控制比较策略\nuvm_comparer comparer = new();\ncomparer.set_max_miscompares(10);       // 最多报告 10 个不匹配\ncomparer.set_severity(UVM_WARNING);     // 不匹配时级别为 WARNING\ncomparer.show_max = 1;                  // 显示最大值\n\nif (!expected.compare(actual, comparer)) begin\n    `uvm_warning(\"CMP\", \"Field mismatch detected\")\nend\n```\n\n## 3. print — 打印\n\n### 使用场景\n\n```systemverilog\n// 场景1：sequence 发送前打印\nclass my_sequence extends uvm_sequence #(my_transaction);\n    task body();\n        my_transaction item;\n        item = my_transaction::type_id::create(\"item\");\n        item.randomize();\n\n        `uvm_info(\"SEQ\", \"Sending transaction:\", UVM_MEDIUM)\n        item.print();  // 自动格式化打印\n    endtask\nendclass\n\n// 场景2：使用不同打印机\nuvm_default_table_printer  table_printer  = new();\nuvm_default_tree_printer   tree_printer   = new();\nuvm_default_line_printer   line_printer   = new();\n\nitem.print(table_printer);  // 表格格式\nitem.print(tree_printer);   // 树形格式\nitem.print(line_printer);   // 单行格式\n\n// 场景3：快速打印一行\n`uvm_info(\"DBG\", item.convert2string(), UVM_HIGH)\n```\n\n## 4. pack / unpack — 打包/解包\n\n### 使用场景\n\n```systemverilog\n// 场景1：跨组件传输（TLM FIFO 内部自动 pack/unpack）\nuvm_tlm_analysis_fifo #(my_transaction) fifo;\n\n// 场景2：手动序列化\nbit packed_data[];\nmy_transaction t1 = new(\"t1\");\nt1.randomize();\n\nt1.pack(packed_data);      // 序列化\n\nmy_transaction t2 = new(\"t2\");\nt2.unpack(packed_data);    // 反序列化\n\n// 场景3：获取打包后的大小\nint size = t1.pack_size();\n`uvm_info(\"PK\", $sformatf(\"Packed size: %0d bits\", size), UVM_MEDIUM)\n\n// 场景4：使用 pack_bytes / pack_ints\nbyte bytes[];\nint  ints[];\nt1.pack_bytes(bytes);\nt1.pack_ints(ints);\n```\n\n## 5. record — 记录到波形\n\n### 使用场景\n\n```systemverilog\n// 在 driver 中记录事务\nclass my_driver extends uvm_driver #(my_transaction);\n    task run_phase(uvm_phase phase);\n        forever begin\n            seq_item_port.get_next_item(req);\n\n            // 开始事务记录\n            accept_tr(req, $realtime);\n            begin_tr(req, 0, \"my_driver\");\n\n            // 驱动总线\n            drive(req);\n\n            // 结束事务记录（此时调用 do_record）\n            end_tr(req, 0);\n\n            seq_item_port.item_done();\n        end\n    endtask\nendclass\n\n// 在波形中可以看到：\n// - 事务的开始/结束时间\n// - addr, data, is_write 等字段的值\n// - 事务之间的关系（父子、先后）\n```\n\n## 6. convert2string — 转字符串\n\n### 使用场景\n\n```systemverilog\n// 场景1：日志输出\n`uvm_info(\"MY_TAG\",\n    $sformatf(\"Processing: %s\", item.convert2string()),\n    UVM_MEDIUM)\n\n// 场景2：错误报告\n`uvm_error(\"ERR\",\n    $sformatf(\"Failed: %s, expected addr=0x%0h\",\n              item.convert2string(), expected_addr))\n\n// 场景3：队列打印\nforeach (queue[i]) begin\n    `uvm_info(\"Q\", $sformatf(\"[%0d] %s\", i, queue[i].convert2string()), UVM_HIGH)\nend\n```\n\n## 使用技巧总结\n\n| 场景 | 推荐函数 | 原因 |\n|------|----------|------|\n| 调试打印 | `print()` | 格式化好，有层次 |\n| 日志输出 | `convert2string()` | 一行，不占位置 |\n| scoreboard 比较 | `compare()` | 自动逐字段比较 |\n| 保存预期值 | `clone()` | 深拷贝，独立对象 |\n| TLM 传输 | 自动 pack/unpack | 框架自动处理 |\n| 波形分析 | `begin_tr/end_tr` | 显示时间轴 |\n\n## 一句话总结\n\n数据处理函数 = UVM 的\"瑞士军刀\"：`copy/clone` 复制备份、`compare` 检查一致性、`print` 调试打印、`pack/unpack` 序列化、`record` 波形追踪、`convert2string` 日志输出。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 修改约束 + type_override 替换实战\n\n## 一句话总结\n\n> **通过继承创建带新约束的子类，再用 `set_type_override` 替换基类，实现在不改动原始代码的情况下修改约束。**\n\n---\n\n## 完整实战例子\n\n### 场景\n\n原始 transaction 的地址约束是 `[0:1023]`，现在需要一个新的测试，地址范围改为 `[2048:4095]`，且突发长度限制为 4 或 8。\n\n### 步骤 1：定义原始 transaction\n\n```systemverilog\nclass base_transaction extends uvm_sequence_item;\n    rand bit [31:0] addr;\n    rand bit [31:0] data;\n    rand bit [ 3:0] burst_len;\n\n    // 原始约束\n    constraint c_addr_range {\n        addr inside {[0:1023]};\n    }\n\n    constraint c_burst_len {\n        burst_len inside {[1:16]};\n    }\n\n    `uvm_object_utils(base_transaction)\n\n    function new(string name = \"base_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n### 步骤 2：创建带新约束的子类\n\n```systemverilog\nclass extended_transaction extends base_transaction;\n    // 新增字段\n    rand bit [1:0] burst_type;\n\n    // 覆盖父类约束：修改地址范围\n    constraint c_addr_range {\n        addr inside {[2048:4095]};\n    }\n\n    // 覆盖父类约束：修改突发长度\n    constraint c_burst_len {\n        burst_len inside {4, 8};\n    }\n\n    // 新增约束\n    constraint c_burst_type {\n        burst_type inside {0, 1, 2};\n    }\n\n    `uvm_object_utils(extended_transaction)\n\n    function new(string name = \"extended_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n### 步骤 3：在 test 中通过 type_override 替换\n\n```systemverilog\nclass my_test extends uvm_test;\n    `uvm_component_utils(my_test)\n\n    function new(string name, uvm_component parent);\n        super.new(name, parent);\n    endfunction\n\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        // 关键：用 factory 替换\n        set_type_override_by_type(\n            base_transaction::get_type(),\n            extended_transaction::get_type()\n        );\n    endfunction\n\n    task run_phase(uvm_phase phase);\n        my_sequence seq;\n\n        phase.raise_objection(this);\n\n        seq = my_sequence::type_id::create(\"seq\");\n        seq.start(env.agent.sequencer);\n\n        phase.drop_objection(this);\n    endtask\nendclass\n```\n\n### 步骤 4：sequence 中无需修改\n\n```systemverilog\nclass my_sequence extends uvm_sequence #(base_transaction);\n    `uvm_object_utils(my_sequence)\n\n    task body();\n        base_transaction item;\n\n        repeat (10) begin\n            // 这行代码看起来创建的是 base_transaction\n            item = base_transaction::type_id::create(\"item\");\n\n            // 但实际上创建的是 extended_transaction！\n            // 约束也用的是 extended_transaction 的版本\n            item.randomize();\n\n            // 如果需要访问子类字段，需要 $cast\n            extended_transaction ext_item;\n            if ($cast(ext_item, item)) begin\n                `uvm_info(\"SEQ\",\n                    $sformatf(\"burst_type=%0d\", ext_item.burst_type),\n                    UVM_MEDIUM)\n            end\n\n            start_item(item);\n            finish_item(item);\n        end\n    endtask\nendclass\n```\n\n## 进阶：动态修改约束 + override\n\n### 方式 1：用 constraint_mode 关闭某个约束\n\n```systemverilog\nclass my_sequence extends uvm_sequence #(base_transaction);\n    task body();\n        base_transaction item;\n\n        item = base_transaction::type_id::create(\"item\");\n\n        // 关闭地址范围约束，自己指定地址\n        item.c_addr_range.constraint_mode(0);\n        item.randomize() with {\n            addr == 32'h2000_0000;\n        };\n\n        start_item(item);\n        finish_item(item);\n    endtask\nendclass\n```\n\n### 方式 2：继承链叠加\n\n```systemverilog\n// 第二层继承\nclass further_transaction extends extended_transaction;\n    // 进一步修改约束\n    constraint c_burst_len {\n        burst_len == 8;  // 只允许 8\n    }\n\n    `uvm_object_utils(further_transaction)\n\n    function new(string name = \"further_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 在另一个 test 中：\nclass another_test extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        set_type_override_by_type(\n            base_transaction::get_type(),\n            further_transaction::get_type()\n        );\n    endfunction\nendclass\n```\n\n### 方式 3：不同 test 用不同约束\n\n```systemverilog\n// test1：正常范围\nclass test_normal extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n        // 不 override，用 base_transaction 的原始约束\n    endfunction\nendclass\n\n// test2：大地址范围\nclass test_large_addr extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n        set_type_override_by_type(\n            base_transaction::get_type(),\n            extended_transaction::get_type()\n        );\n    endfunction\nendclass\n\n// test3：极限约束\nclass test_extreme extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n        set_type_override_by_type(\n            base_transaction::get_type(),\n            further_transaction::get_type()\n        );\n    endfunction\nendclass\n```\n\n## 完整流程总结\n\n```\n原始 transaction (base)\n    │\n    ├── 继承 → 子类 transaction (extended)\n    │              │\n    │              ├── 覆盖父类约束\n    │              ├── 添加新约束\n    │              └── 添加新字段\n    │\n    └── test::build_phase\n            │\n            └── set_type_override_by_type(base, extended)\n                    │\n                    └── 所有 base::create() → 实际创建 extended\n```\n\n## 关键注意事项\n\n| 注意点 | 说明 |\n|--------|------|\n| 约束覆盖 | 子类同名约束会覆盖父类约束，不是合并 |\n| 访问子类字段 | 需要用 `$cast` 转换句柄类型 |\n| 时机 | override 必须在 `create()` 之前调用 |\n| 顺序 | 子类约束可以和父类约束共存（不同名时） |\n| 调试 | 用 `get_type_name()` 确认实际创建的是哪个类 |\n\n## 一句话总结\n\n修改约束 + type_override 的标准流程：继承基类 → 覆盖/新增约束 → `set_type_override_by_type` 替换 → sequence 代码不变，约束自动切换。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 参数化的数据类 (Parameterized Transaction)\n\n## 一句话总结\n\n> **参数化让一个 transaction 类适配不同位宽、不同数据类型，避免为每种位宽都写一个类。**\n\n---\n\n## 为什么需要参数化\n\n不用参数化时，你需要为每种位宽写一个类：\n\n```systemverilog\n// ❌ 每种位宽一个类，大量重复代码\nclass transaction_8  extends uvm_sequence_item;  rand bit [ 7:0] data; endclass\nclass transaction_16 extends uvm_sequence_item;  rand bit [15:0] data; endclass\nclass transaction_32 extends uvm_sequence_item;  rand bit [31:0] data; endclass\nclass transaction_64 extends uvm_sequence_item;  rand bit [63:0] data; endclass\n```\n\n用参数化后，一个类搞定：\n\n```systemverilog\n// ✅ 一个参数化类，覆盖所有位宽\nclass transaction #(int WIDTH = 32) extends uvm_sequence_item;\n    rand bit [WIDTH-1:0] data;\nendclass\n```\n\n## 参数化 transaction 基本写法\n\n### 示例 1：数值参数\n\n```systemverilog\nclass my_transaction #(\n    int ADDR_WIDTH = 32,\n    int DATA_WIDTH = 32\n) extends uvm_sequence_item;\n\n    rand bit [ADDR_WIDTH-1:0] addr;\n    rand bit [DATA_WIDTH-1:0] data;\n\n    `uvm_object_param_utils_begin(my_transaction #(ADDR_WIDTH, DATA_WIDTH))\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 使用：\ntypedef my_transaction #(32, 32)  trans_32;\ntypedef my_transaction #(64, 64)  trans_64;\ntypedef my_transaction #(32, 64)  trans_32_64;  // 地址 32，数据 64\n```\n\n### 示例 2：类型参数\n\n```systemverilog\nclass my_transaction #(\n    type DATA_T = bit [31:0],\n    type ADDR_T = bit [31:0]\n) extends uvm_sequence_item;\n\n    rand ADDR_T addr;\n    rand DATA_T data;\n\n    `uvm_object_param_utils_begin(my_transaction #(DATA_T, ADDR_T))\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 使用：\ntypedef my_transaction #(bit [15:0], bit [31:0]) narrow_trans;\ntypedef my_transaction #(bit [63:0], bit [63:0]) wide_trans;\ntypedef my_transaction #(int, int)              int_trans;\n```\n\n### 示例 3：带约束的参数化\n\n```systemverilog\nclass my_transaction #(\n    int ADDR_WIDTH = 32,\n    int MAX_BURST = 16\n) extends uvm_sequence_item;\n\n    rand bit [ADDR_WIDTH-1:0] addr;\n    rand bit [31:0]           data;\n    rand int                  burst_len;\n\n    constraint c_burst_len {\n        burst_len inside {[1:MAX_BURST]};  // 参数影响约束\n    }\n\n    constraint c_addr_align {\n        addr[1:0] == 0;\n    }\n\n    `uvm_object_param_utils_begin(my_transaction #(ADDR_WIDTH, MAX_BURST))\n        `uvm_field_int(addr,      UVM_ALL_ON)\n        `uvm_field_int(data,      UVM_ALL_ON)\n        `uvm_field_int(burst_len, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"my_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 不同测试用不同参数\ntypedef my_transaction #(32, 16)  trans_standard;\ntypedef my_transaction #(64, 256) trans_large;\n```\n\n## 参数化 + 继承\n\n```systemverilog\n// 基类：参数化\nclass base_transaction #(int WIDTH = 32) extends uvm_sequence_item;\n    rand bit [WIDTH-1:0] addr;\n    rand bit [WIDTH-1:0] data;\n\n    `uvm_object_param_utils_begin(base_transaction #(WIDTH))\n        `uvm_field_int(addr, UVM_ALL_ON)\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"base_transaction\");\n        super.new(name);\n    endfunction\nendclass\n\n// 子类：继承参数化基类，添加新字段\nclass extended_transaction #(int WIDTH = 32) extends base_transaction #(WIDTH);\n    rand bit [3:0] burst_len;\n\n    `uvm_object_param_utils_begin(extended_transaction #(WIDTH))\n        `uvm_field_int(burst_len, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"extended_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n## 注意事项\n\n| 注意点 | 说明 |\n|--------|------|\n| 参数化宏 | 必须用 `uvm_object_param_utils_begin`，不能用 `uvm_object_utils_begin` |\n| typedef | 参数化类实例化时建议用 `typedef` 起别名，方便后续使用 |\n| 参数传递 | 子类继承参数化父类时，要传递参数：`extends base_transaction #(WIDTH)` |\n| 约束中使用参数 | 参数可以出现在约束中，实现灵活的约束配置 |\n| 默认值 | 给参数设默认值，不传参时使用默认 |\n\n## 一句话总结\n\n参数化 transaction = 一个类模板覆盖所有位宽/类型组合，用 `#(...)` 定义参数，`typedef` 起别名，`uvm_object_param_utils_begin` 注册。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 参数化对象的宏定义与参数传递\n\n## 一句话总结\n\n> **参数化类必须用 `param_utils` 系列宏，且参数传递方式因场景而异——factory 创建、typedef 别名、sequence 参数化各有写法。**\n\n---\n\n## 宏选择对照表\n\n| 类类型 | 无 field | 有 field |\n|--------|----------|----------|\n| 普通类 | `uvm_object_utils(T)` | `uvm_object_utils_begin(T)` |\n| 参数化类 | `uvm_object_param_utils(T)` | `uvm_object_param_utils_begin(T)` |\n| 普通组件 | `uvm_component_utils(T)` | `uvm_component_utils_begin(T)` |\n| 参数化组件 | `uvm_component_param_utils(T)` | `uvm_component_param_utils_begin(T)` |\n\n## 参数化宏的写法\n\n### 关键：宏参数必须包含完整参数列表\n\n```systemverilog\nclass my_transaction #(int W = 32, type T = bit) extends uvm_sequence_item;\n    rand T data;\n\n    // ✅ 正确：宏参数包含 #(W, T)\n    `uvm_object_param_utils_begin(my_transaction #(W, T))\n        `uvm_field_int(data, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    // ❌ 错误：忘记参数\n    // `uvm_object_utils_begin(my_transaction)  // 编译报错！\nendclass\n```\n\n## 参数传递的三种场景\n\n### 场景 1：typedef 起别名（最常用）\n\n```systemverilog\n// 用 typedef 固定参数，创建\"具体类型\"\ntypedef my_transaction #(32, bit [7:0]) trans_8bit;\ntypedef my_transaction #(64, bit [31:0]) trans_32bit;\n\n// 后续使用 typedef 的别名\nclass my_sequence extends uvm_sequence #(trans_8bit);\n    task body();\n        trans_8bit item;\n        item = trans_8bit::type_id::create(\"item\");\n        item.randomize();\n    endtask\nendclass\n```\n\n### 场景 2：factory 创建时传参\n\n```systemverilog\n// 方式 A：直接用 type_id::create（参数已通过 typedef 固定）\ntypedef my_transaction #(32) trans32;\ntrans32 item;\nitem = trans32::type_id::create(\"item\");\n\n// 方式 B：手动 new（不经过 factory）\nmy_transaction #(32) item;\nitem = new(\"item\");\n\n// 方式 C：在 factory override 中\nset_type_override_by_type(\n    my_transaction #(32)::get_type(),\n    extended_transaction #(32)::get_type()\n);\n```\n\n### 场景 3：参数化 sequence\n\n```systemverilog\n// sequence 本身也可以是参数化的\nclass my_sequence #(type T = my_transaction) extends uvm_sequence #(T);\n    `uvm_object_param_utils(my_sequence #(T))\n\n    task body();\n        T item;\n        repeat (10) begin\n            item = T::type_id::create(\"item\");\n            item.randomize();\n            start_item(item);\n            finish_item(item);\n        end\n    endtask\nendclass\n\n// 使用：\ntypedef my_sequence #(trans_8bit)  seq_8bit;\ntypedef my_sequence #(trans_32bit) seq_32bit;\n```\n\n## 参数化 driver 也用参数化 transaction\n\n```systemverilog\nclass my_driver #(type T = my_transaction) extends uvm_driver #(T);\n    `uvm_component_param_utils(my_driver #(T))\n\n    function new(string name, uvm_component parent);\n        super.new(name, parent);\n    endfunction\n\n    task run_phase(uvm_phase phase);\n        forever begin\n            seq_item_port.get_next_item(req);\n            drive(req);\n            seq_item_port.item_done();\n        end\n    endtask\nendclass\n```\n\n## 完整参数化环境示例\n\n```systemverilog\n// 参数化 transaction\nclass my_transaction #(int W = 32) extends uvm_sequence_item;\n    rand bit [W-1:0] addr;\n    `uvm_object_param_utils_begin(my_transaction #(W))\n        `uvm_field_int(addr, UVM_ALL_ON)\n    `uvm_object_utils_end\n    function new(string n = \"my_transaction\"); super.new(n); endfunction\nendclass\n\n// 参数化 sequence\ntypedef my_transaction #(64) trans64;\n\ntypedef my_sequence #(trans64) seq64;\n\n// 参数化 driver\ntypedef my_driver #(trans64) drv64;\n\n// 在 test 中组装\nclass my_test extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        // 用 typedef 的别名，代码清晰\n        seq64 seq;\n        seq = seq64::type_id::create(\"seq\");\n    endfunction\nendclass\n```\n\n## 常见错误\n\n| 错误 | 原因 | 修正 |\n|------|------|------|\n| 编译报错找不到 type_id | 没用 `param_utils` | 改用 `uvm_object_param_utils_begin` |\n| override 不生效 | 参数不匹配 | 确保 override 类型参数一致 |\n| 句柄不兼容 | 忘记 typedef | 用 `typedef` 固定参数后统一使用 |\n\n## 一句话总结\n\n参数化类 = `param_utils` 宏 + `typedef` 别名 + `#(参数)` 传递，factory 创建和 override 时参数必须一致。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "# 数据建模的重用性：给数据分层\n\n## 一句话总结\n\n> **数据分层 = 把 transaction 按抽象级别拆成多层，每层关注不同职责，上层复用下层，实现\"改一个字段，用所有场景\"。**\n\n---\n\n## 为什么需要分层\n\n### 不分层的问题\n\n```systemverilog\n// ❌ 一个 transaction 包含所有层级的字段，臃肿且难以复用\nclass flat_transaction extends uvm_sequence_item;\n    // 物理层\n    rand bit [31:0] phy_addr;\n    rand bit [ 3:0] phy_burst;\n\n    // 传输层\n    rand bit [31:0] trans_src_addr;\n    rand bit [31:0] trans_dst_addr;\n\n    // 应用层\n    rand bit [31:0] app_cmd;\n    rand bit [31:0] app_data;\n    rand bit [31:0] app_status;\nendclass\n```\n\n问题：\n- 想复用物理层字段到另一个协议？不行，全都混在一起\n- 想单独修改传输层约束？牵一发动全身\n- 不同验证工程师想扩展不同层？互相冲突\n\n## 三层数据模型\n\n```\n┌─────────────────────────────────────┐\n│  应用层 (Application Layer)         │\n│  cmd, data, status, priority        │\n│  继承传输层                          │\n├─────────────────────────────────────┤\n│  传输层 (Transport Layer)           │\n│  src_addr, dst_addr, length, id     │\n│  继承物理层                          │\n├─────────────────────────────────────┤\n│  物理层 (Physical Layer)            │\n│  addr, data, burst_len, burst_type  │\n│  继承 uvm_sequence_item             │\n└─────────────────────────────────────┘\n```\n\n## 实现：分层继承\n\n### 第 1 层：物理层（最底层）\n\n```systemverilog\nclass phy_transaction extends uvm_sequence_item;\n    rand bit [31:0] phy_addr;\n    rand bit [31:0] phy_data;\n    rand bit [ 3:0] burst_len;\n    rand bit [ 1:0] burst_type;\n\n    constraint c_phy {\n        phy_addr[1:0] == 0;              // 地址对齐\n        burst_len inside {[1:16]};\n    }\n\n    `uvm_object_utils_begin(phy_transaction)\n        `uvm_field_int(phy_addr,   UVM_ALL_ON)\n        `uvm_field_int(phy_data,   UVM_ALL_ON)\n        `uvm_field_int(burst_len,  UVM_ALL_ON)\n        `uvm_field_int(burst_type, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"phy_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n### 第 2 层：传输层（继承物理层）\n\n```systemverilog\nclass transport_transaction extends phy_transaction;\n    rand bit [31:0] src_addr;\n    rand bit [31:0] dst_addr;\n    rand bit [15:0] packet_id;\n    rand bit [ 7:0] length;\n\n    constraint c_transport {\n        src_addr != dst_addr;\n        length > 0;\n    }\n\n    `uvm_object_utils_begin(transport_transaction)\n        `uvm_field_int(src_addr,  UVM_ALL_ON)\n        `uvm_field_int(dst_addr,  UVM_ALL_ON)\n        `uvm_field_int(packet_id, UVM_ALL_ON)\n        `uvm_field_int(length,    UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"transport_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n### 第 3 层：应用层（继承传输层）\n\n```systemverilog\nclass app_transaction extends transport_transaction;\n    rand bit [31:0] cmd;\n    rand bit [31:0] app_data;\n    rand bit [ 7:0] status;\n    rand bit [ 1:0] priority;\n\n    constraint c_app {\n        cmd inside {0, 1, 2, 3};\n        priority dist {0:=30, 1:=50, 2:=15, 3:=5};\n    }\n\n    `uvm_object_utils_begin(app_transaction)\n        `uvm_field_int(cmd,      UVM_ALL_ON)\n        `uvm_field_int(app_data, UVM_ALL_ON)\n        `uvm_field_int(status,   UVM_ALL_ON)\n        `uvm_field_int(priority, UVM_ALL_ON)\n    `uvm_object_utils_end\n\n    function new(string name = \"app_transaction\");\n        super.new(name);\n    endfunction\nendclass\n```\n\n## 分层后的好处\n\n### 1. 按需选择层级\n\n```systemverilog\n// 只测物理层 → 用 phy_transaction\nclass phy_sequence extends uvm_sequence #(phy_transaction);\n    task body();\n        phy_transaction item;\n        item = phy_transaction::type_id::create(\"item\");\n        item.randomize();  // 只有物理层约束\n    endtask\nendclass\n\n// 测传输层 → 用 transport_transaction\nclass transport_sequence extends uvm_sequence #(transport_transaction);\n    task body();\n        transport_transaction item;\n        item = transport_transaction::type_id::create(\"item\");\n        item.randomize();  // 物理层 + 传输层约束\n    endtask\nendclass\n\n// 端到端测试 → 用 app_transaction\nclass app_sequence extends uvm_sequence #(app_transaction);\n    task body();\n        app_transaction item;\n        item = app_transaction::type_id::create(\"item\");\n        item.randomize();  // 全部三层约束\n    endtask\nendclass\n```\n\n### 2. 独立扩展每一层\n\n```systemverilog\n// 只扩展物理层：增加 ECC 字段\nclass phy_with_ecc extends phy_transaction;\n    rand bit [7:0] ecc_code;\n\n    constraint c_ecc { ecc_code != 0; }\n\n    `uvm_object_utils(phy_with_ecc)\n    function new(string n = \"phy_with_ecc\"); super.new(n); endfunction\nendclass\n\n// 只扩展传输层：增加 QOS 字段\nclass transport_with_qos extends transport_transaction;\n    rand bit [3:0] qos;\n\n    constraint c_qos { qos inside {0, 1, 4, 8}; }\n\n    `uvm_object_utils(transport_with_qos)\n    function new(string n = \"transport_with_qos\"); super.new(n); endfunction\nendclass\n```\n\n### 3. 不同层可以独立 override\n\n```systemverilog\nclass my_test extends uvm_test;\n    function void build_phase(uvm_phase phase);\n        super.build_phase(phase);\n\n        // 物理层替换：所有用到 phy 的地方都换\n        set_type_override_by_type(\n            phy_transaction::get_type(),\n            phy_with_ecc::get_type()\n        );\n\n        // 传输层替换：只影响传输层及以上\n        set_type_override_by_type(\n            transport_transaction::get_type(),\n            transport_with_qos::get_type()\n        );\n    endfunction\nendclass\n```\n\n## 分层设计原则\n\n| 原则 | 说明 |\n|------|------|\n| **单一职责** | 每层只关注自己的域（物理/传输/应用） |\n| **自底向上** | 下层不依赖上层，上层继承下层 |\n| **约束分层** | 每层约束只管理自己层的字段 |\n| **可替换** | 任一层可以独立被 factory override |\n| **可复用** | 物理层可用于 AHB、AXI、PCIe 等不同协议 |\n\n## 分层 + 参数化组合\n\n```systemverilog\n// 参数化物理层\nclass phy_transaction #(int W = 32) extends uvm_sequence_item;\n    rand bit [W-1:0] phy_addr;\n    rand bit [W-1:0] phy_data;\n    `uvm_object_param_utils_begin(phy_transaction #(W))\n        `uvm_field_int(phy_addr, UVM_ALL_ON)\n        `uvm_field_int(phy_data, UVM_ALL_ON)\n    `uvm_object_utils_end\n    function new(string n = \"phy_transaction\"); super.new(n); endfunction\nendclass\n\n// 参数化传输层\nclass transport_transaction #(int W = 32) extends phy_transaction #(W);\n    rand bit [W-1:0] src_addr, dst_addr;\n    `uvm_object_param_utils_begin(transport_transaction #(W))\n        `uvm_field_int(src_addr, UVM_ALL_ON)\n        `uvm_field_int(dst_addr, UVM_ALL_ON)\n    `uvm_object_utils_end\n    function new(string n = \"transport_transaction\"); super.new(n); endfunction\nendclass\n\n// 使用\ntypedef transport_transaction #(64) trans64;\ntrans64 item = trans64::type_id::create(\"item\");\n// item 拥有 64 位宽的 phy_addr, phy_data, src_addr, dst_addr！\n```\n\n## 一句话总结\n\n数据分层 = 物理层→传输层→应用层逐层继承，每层只关注自己的职责，改一层不影响其他层，配合参数化实现最大复用。"
    }
  ]
}