{
  "version": 1,
  "cells": [
    {
      "kind": 1,
      "language": "markdown",
      "value": "# Virtual Methods（虚方法）\n\n在面向对象编程中，virtual method（虚方法）指的是：\n\n- 基类中声明一个方法\n- 子类可以重写同名方法\n- 通过基类句柄调用时，实际执行的是子类的实现\n\n这就是多态（polymorphism）的核心机制。\n\n## 例子\n\n```systemverilog\nclass animal;\n  virtual function void speak();\n    $display(\"animal says...\");\n  endfunction\nendclass\n\nclass dog extends animal;\n  virtual function void speak();\n    $display(\"dog says: wang wang\");\n  endfunction\nendclass\n\nclass cat extends animal;\n  virtual function void speak();\n    $display(\"cat says: meow\");\n  endfunction\nendclass\n```\n\n```systemverilog\nanimal a;\ndog d = new();\ncat c = new();\n\na = d;\na.speak();  // 输出：dog says: wang wang\n\na = c;\na.speak();  // 输出：cat says: meow\n```\n\n## 重点\n\n- `virtual` 让方法能够被子类覆盖\n- 调用者不需要知道对象的真实类型\n- 这样代码更灵活，便于扩展\n\n## 在 UVM 中的意义\n\nUVM 里很多方法都是 virtual 的，例如：\n\n- `build_phase`\n- `connect_phase`\n- `run_phase`\n- `report_phase`\n\n这使得每个子类都能按自己的需求重写行为，而测试环境统一调用这些接口时，不需要手动判断具体对象类型。\n\n## 一句话总结\n\nVirtual method 就是：同一个方法名，在不同对象上执行时，可以表现出不同的具体行为。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 在 class 外部定义子函数\n\n在 SystemVerilog 里，类中的方法也可以不直接写在 class 内部，而是在 class 外部单独定义。这样通常用于把实现代码分离出来，代码更清晰，尤其在大项目中很常见。\n\n### 语法格式\n\n```systemverilog\nclass 类名;\n  // 先声明方法\n  function 返回类型 方法名(参数列表);\n  endfunction\nendclass\n\n// 在类外实现\nfunction 返回类型 类名::方法名(参数列表);\n  // 具体逻辑\nendfunction\n```\n\n### 例子\n\n```systemverilog\nclass packet;\n  int data;\n\n  function new(int d);\n    data = d;\n  endfunction\n\n  function void print();\n    $display(\"data = %0d\", data);\n  endfunction\nendclass\n\nfunction void packet::print();\n  $display(\"外部实现：data = %0d\", data);\nendfunction\n```\n\n### 使用方式\n\n```systemverilog\nmodule tb;\n  initial begin\n    packet p = new(10);\n    p.print();\n  end\nendmodule\n```\n\n### 关键点\n\n- `class_name::method_name` 表示这是类的成员函数\n- 这里的 `print` 仍然属于 `packet` 类，调用方式不变\n- 只是在定义时把实现放到类外面\n- 这样更利于代码管理和模块化\n\n### 什么时候用\n\n当类比较大、方法很多、逻辑较复杂时，可以把成员函数拆到类外实现，避免 class 内部代码过长。\n\n### 一句话总结\n\n在 class 外部定义子函数，就是把类里的方法声明在 class 内，实际实现写到 `类名::方法名` 的形式中。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 参数化的类（Parameterized Class）\n\n在 SystemVerilog 中，类也可以像模块、接口一样做参数化。也就是说，类在定义时可以带一些参数，这些参数决定类中的数据类型、位宽或者具体值。\n\n这样做的好处是：\n\n- 可以复用同一套类定义\n- 不需要写很多类似的类\n- 更容易适配不同位宽或不同数据类型\n\n### 基本语法\n\n```systemverilog\nclass packet #(type T = int, int WIDTH = 8);\n  T data;\n  bit [WIDTH-1:0] value;\n\n  function new(T d, bit [WIDTH-1:0] v);\n    data = d;\n    value = v;\n  endfunction\n\n  function void print();\n    $display(\"data = %0d, value = %0d\", data, value);\n  endfunction\nendclass\n```\n\n### 使用方式\n\n```systemverilog\nmodule tb;\n  initial begin\n    packet #(bit [7:0], 8) p1 = new(8'h5A, 8'hA5);\n    p1.print();\n\n    packet #(int, 16) p2 = new(123, 16'h1234);\n    p2.print();\n  end\nendmodule\n```\n\n### 说明\n\n- `#(type T = int, int WIDTH = 8)` 表示这个类有两个参数\n- `type T` 是类型参数，用来指定数据类型\n- `int WIDTH` 是数值参数，用来指定位宽\n- 实例化时可以直接传入不同的参数，生成不同版本的类\n\n### 典型应用\n\n参数化类在 UVM 中非常常见，比如：\n\n- 泛型 transaction\n- 不同位宽的 FIFO/queue 封装\n- 各种不同类型的数据对象\n\n### 一句话总结\n\n参数化类就是：定义一套通用类模板，然后通过参数指定其数据类型或位宽，生成不同版本的类实例。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 声明（Declaration）\n\n在 SystemVerilog 里，“声明”指的是告诉编译器：\n\n- 这个名字是什么\n- 它的类型是什么\n- 它属于什么范围\n\n简单说，声明就是“先告诉程序我准备要用一个变量/类/函数，它是什么东西”。\n\n### 1. 变量声明\n\n```systemverilog\nint a;          // 声明一个 32 位整数变量 a\nbit [7:0] b;    // 声明一个 8 位 bit 型变量 b\nlogic c;        // 声明一个 1 bit 逻辑变量 c\n```\n\n这里的关键是：\n\n- `a` 是变量名\n- `int` 是变量类型\n- 编译器知道 `a` 可以存放整数\n\n### 2. 类声明\n\n```systemverilog\nclass my_class;\n  int x;\n\n  function void set_x(int value);\n    x = value;\n  endfunction\nendclass\n```\n\n这里说明了：\n\n- `my_class` 是一个类名\n- `x` 是这个类里的成员变量\n- `set_x` 是这个类里的成员函数\n\n### 3. 对象声明\n\n```systemverilog\nmy_class obj;\nobj = new();\n```\n\n这里 `obj` 是一个对象句柄/实例名，类型是 `my_class`。\n\n注意：\n\n- 先声明 `obj`\n- 再用 `new()` 创建对象\n\n### 4. 函数声明\n\n```systemverilog\nfunction int add(int a, int b);\n  return a + b;\nendfunction\n```\n\n这里的意思是：\n\n- 这个函数名叫 `add`\n- 它接收两个 `int` 参数\n- 它返回一个 `int`\n\n### 5. 重要区别：声明 vs 定义\n\n很多人容易混淆这两个概念：\n\n- 声明：告诉编译器“有这个东西”\n- 定义：告诉编译器“它具体怎么实现”\n\n例如：\n\n```systemverilog\nclass my_class;\n  function void foo();\n  endfunction\nendclass\n```\n\n这里 `foo` 是声明和定义都在同一处。\n\n但如果写成：\n\n```systemverilog\nclass my_class;\n  function void foo();\n  endfunction\nendclass\n\nfunction void my_class::foo();\n  $display(\"hello\");\nendfunction\n```\n\n这种就是：\n\n- 在 class 里先声明方法\n- 在 class 外部再实现方法\n\n### 6. 为什么要理解声明\n\n理解声明是写 SystemVerilog/UVM 的基础，因为它决定了：\n\n- 我们的对象是什么类型\n- 这个方法是否存在\n- 这个变量是否能被使用\n- 类之间如何继承和调用\n\n### 一句话总结\n\n声明就是“先说明某个名字、类型和作用”，让编译器知道这个东西存在；之后再根据需要去创建对象、实现方法、赋值或调用它。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 静态变量与静态方法（static variable / static method）\n\n在 SystemVerilog 类中，默认成员变量和方法都是“实例成员”，也就是每个对象都有一份自己的拷贝。\n\n而 `static` 修饰符表示：\n\n- 这个变量或方法属于“类”，不属于某一个具体对象\n- 所有对象共享同一个静态成员\n- 不需要创建对象也可以访问（如果是 `static` 方法）\n\n### 1. 静态变量\n\n```systemverilog\nclass counter;\n  static int count = 0;\n\n  function new();\n    count++;\n  endfunction\nendclass\n```\n\n例子：\n\n```systemverilog\nmodule tb;\n  initial begin\n    counter c1 = new();\n    counter c2 = new();\n\n    $display(\"count = %0d\", counter::count);\n    $display(\"count = %0d\", c1.count);\n    $display(\"count = %0d\", c2.count);\n  end\nendmodule\n```\n\n输出中 `count` 是共享的，所以会累计增加。\n\n### 2. 关键点\n\n- `static int count;` 只有一份\n- 不同对象访问到的是同一个值\n- 适合用来记录全局计数、状态、总数等\n\n### 3. 静态方法\n\n```systemverilog\nclass math_ops;\n  static function int add(int a, int b);\n    return a + b;\n  endfunction\nendclass\n```\n\n调用方式：\n\n```systemverilog\nmodule tb;\n  initial begin\n    int x;\n    x = math_ops::add(3, 5);\n    $display(\"x = %0d\", x);\n  end\nendmodule\n```\n\n这里不需要实例化对象，也能直接调用：\n\n```systemverilog\nmath_ops::add(3, 5)\n```\n\n### 4. 静态方法不能访问非静态成员\n\n```systemverilog\nclass demo;\n  int x;\n\n  static function void print_x();\n    $display(\"x = %0d\", x); // 错误，不能访问非静态成员\n  endfunction\nendclass\n```\n\n原因是：\n\n- 静态方法属于类，不绑定到某个对象实例\n- 它没有“当前对象”这个上下文\n- 所以不能直接访问实例变量 `x`\n\n### 5. 什么时候用静态成员\n\n适合场景：\n\n- 全局计数器\n- 共享参数\n- 工具函数\n- 不依赖某个具体对象状态的方法\n\n### 6. 一句话总结\n\n`static` 表示“属于类而不是对象”：\n\n- 静态变量：所有对象共享一份\n- 静态方法：可以直接用类名调用，不依赖对象实例\n\n这在 UVM 中常用于一些全局配置、计数器和通用工具函数。"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## Singleton Class（单例类）\n\nSingleton（单例模式）指的是：一个类在整个程序中只能有一个实例对象。\n\n它通常用于：\n\n- 全局配置对象\n- 日志管理器\n- 资源管理器\n- 共享环境对象\n\n在 UVM 里，很多组件也会用到“单例”的思路，确保全局唯一。\n\n### 1. 基本思想\n\n类中提供一个静态的对象句柄，并提供一个静态方法来获取这个唯一实例。\n\n```systemverilog\nclass config_db;\n  static config_db inst;\n\n  int mode;\n\n  private function new();\n    mode = 0;\n  endfunction\n\n  static function config_db get_inst();\n    if (inst == null)\n      inst = new();\n    return inst;\n  endfunction\nendclass\n```\n\n### 2. 为什么这样设计\n\n- `static config_db inst;` 表示这个类只有一个全局对象\n- `get_inst()` 在第一次调用时创建对象\n- 后面再调用时直接返回同一个对象\n\n### 3. 使用方式\n\n```systemverilog\nmodule tb;\n  initial begin\n    config_db cfg1;\n    config_db cfg2;\n\n    cfg1 = config_db::get_inst();\n    cfg2 = config_db::get_inst();\n\n    cfg1.mode = 1;\n    $display(\"cfg1.mode = %0d\", cfg1.mode);\n    $display(\"cfg2.mode = %0d\", cfg2.mode);\n\n    if (cfg1 == cfg2)\n      $display(\"same instance\");\n  end\nendmodule\n```\n\n运行结果说明：\n\n- `cfg1` 和 `cfg2` 指向同一个对象\n- 修改其中一个，另一个也会看到同样的值\n\n### 4. `private function new()` 的作用\n\n这里 `new()` 被声明为 `private`，作用是：\n\n- 防止外部代码直接用 `new()` 创建第二个对象\n- 只允许通过 `get_inst()` 创建唯一实例\n\n这样才能保证单例性。\n\n### 5. 关键点总结\n\n- 单例模式保证对象唯一\n- 用 `static` 变量保存唯一实例\n- 用 `static` 方法返回唯一实例\n- 用 `private new()` 禁止外部随意构造\n\n### 6. 一句话总结\n\nSingleton Class 就是：一个类只允许创建一个对象，所有访问都通过同一个全局入口获取，这样可以统一管理共享状态。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "`static` 表示“属于类而不是对象”：\n\n- 静态变量：所有对象共享一份\n- 静态方法：可以直接用类名调用，不依赖对象实例\n\n这在 UVM 中常用于一些全局配置、计数器和通用工具函数。\n\n## 单列对象（Singleton Object）\n\n很多人会把“单列对象”和“单例类”混在一起，其实它们的侧重点不一样。\n\n### 1. 单例类\n\n单例类是指“这个类的设计目标是只允许一个实例”。\n\n它是一种类的设计模式，强调“类的创建方式”。\n\n```systemverilog\nclass config_db;\n  static config_db inst;\n\n  private function new();\n  endfunction\n\n  static function config_db get_inst();\n    if (inst == null)\n      inst = new();\n    return inst;\n  endfunction\nendclass\n```\n\n这里的关键是：\n\n- 这个类只会生产一个对象\n- 通过 `get_inst()` 获取对象\n- 外部无法直接 `new` 一个新的实例\n\n### 2. 单列对象\n\n“单列对象”更偏向“某个对象本身只有一个实例”，重点在对象的存在方式，而不一定强制要求代码结构上写成单例类。\n\n例如：\n\n```systemverilog\nclass env;\n  static env single_env;\nendclass\n```\n\n也就是说：\n\n- 这个对象在系统中只有一份\n- 用一个全局变量/静态句柄来保存它\n- 其他地方只访问这个唯一对象\n\n### 3. 本质区别\n\n- 单例类：强调“类设计为单实例”\n- 单列对象：强调“某个对象在运行时只有一个实例”\n\n它们通常是同一个思想的不同表述，但从工程角度看：\n\n- 单例类更规范更标准\n- 单列对象更口语化，描述状态更直观\n\n### 4. 一个更直白的例子\n\n```systemverilog\nclass cpu_env;\n  static cpu_env inst;\n\n  int clk_period;\n\n  private function new();\n    clk_period = 10;\n  endfunction\n\n  static function cpu_env get_inst();\n    if (inst == null)\n      inst = new();\n    return inst;\n  endfunction\nendclass\n```\n\n在代码中：\n\n```systemverilog\ncpu_env e1 = cpu_env::get_inst();\ncpu_env e2 = cpu_env::get_inst();\n```\n\n这里 `e1` 和 `e2` 指向同一个对象，因此它们共享同一份状态。\n\n### 5. 一句话总结\n\n“单列对象”是强调对象唯一；“单例类”是强调用类的设计方式确保唯一。两者目标一致，都是为了让全局共享状态只有一份。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## Proxy Class（代理类 / 代理模式）\n\nProxy Class（代理类）通常是指：\n\n- 一个类充当另一个类的“代理”\n- 对外提供同样的接口\n- 通过它来控制、转发或管理对真实对象的访问\n\n它和单例类、继承类不一样，重点在于“中间层”或者“替身”的角色。\n\n### 1. 直观理解\n\n假设你有一个真实对象，直接访问它可能比较复杂，或者不方便。于是你再包一层代理类，所有访问都经过代理。\n\n```systemverilog\nclass real_service;\n  function void do_task();\n    $display(\"真实服务在执行\");\n  endfunction\nendclass\n\nclass proxy_service;\n  real_service rs;\n\n  function new();\n    rs = new();\n  endfunction\n\n  function void do_task();\n    $display(\"代理先处理一些事情\");\n    rs.do_task();\n  endfunction\nendclass\n```\n\n使用方法：\n\n```systemverilog\nmodule tb;\n  initial begin\n    proxy_service p = new();\n    p.do_task();\n  end\nendmodule\n```\n\n这个例子中：\n\n- `proxy_service` 是代理\n- `real_service` 是真实对象\n- 调用 `p.do_task()` 时，实际上经历了代理层\n\n### 2. 代理类的作用\n\n代理类通常用于：\n\n- 限制对真实对象的直接访问\n- 增加额外检查或日志\n- 管理对象生命周期\n- 让接口更统一、更容易扩展\n\n### 3. 在 UVM 里的理解\n\nUVM 里也有“代理”的概念，但它通常不是传统面向对象里的“proxy class”这么抽象，而是更具体的“driver/monitor/agent”角色。\n\n例如：\n\n- `uvm_driver` 负责驱动测试\n- `uvm_monitor` 负责观察\n- `uvm_agent` 把它们组合在一起\n\n这里的 `agent` 也相当于一个“中间层/协调层”，控制底层真实行为的访问。\n\n### 4. 代理类和继承的区别\n\n代理类不是简单的“子类替代父类”，而是：\n\n- 通过持有真实对象\n- 在外部封装一层控制\n\n而继承是：\n\n- 子类直接扩展父类\n- 共享父类的接口和行为\n\n### 5. 一个更接近 UVM 的例子\n\n```systemverilog\nclass data_source;\n  function void send();\n    $display(\"发送数据\");\n  endfunction\nendclass\n\nclass data_proxy;\n  data_source src;\n\n  function new();\n    src = new();\n  endfunction\n\n  function void send_with_check();\n    $display(\"检查参数合法性\");\n    src.send();\n  endfunction\nendclass\n```\n\n这类代理通常用于：\n\n- 参数校验\n- 访问控制\n- 统一封装\n- 让真实对象不直接暴露给外部\n\n### 6. 一句话总结\n\nProxy Class 就是一个“中间层类”，它代替真实对象对外提供服务，并在中间添加控制、检查或转发逻辑。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## create 方法（UVM 中最重要的对象创建方式）\n\n在 UVM 里，`create` 不是普通的 `new()` 调用，它是一种更规范、更适合对象层次结构的构造方式。\n\n它的核心思想是：\n\n- 通过基类类型创建对象\n- 自动保留对象类型信息\n- 方便在继承体系中做对象创建和重写\n\n### 1. 普通 `new()` 和 `create()` 的区别\n\n普通写法：\n\n```systemverilog\nclass packet;\n  function new();\n  endfunction\nendclass\n\nmodule tb;\n  initial begin\n    packet p = new();\n  end\nendmodule\n```\n\n这种方式很直接，但在 UVM 中不够通用，因为它不能很好地配合“类型扩展”和“工厂机制”。\n\nUVM 里的写法：\n\n```systemverilog\nclass packet extends uvm_object;\n  `uvm_object_utils(packet)\n\n  function new(string name = \"packet\");\n    super.new(name);\n  endfunction\nendclass\n```\n\n然后创建对象：\n\n```systemverilog\npacket p;\np = packet::type_id::create(\"p\");\n```\n\n### 2. `create` 的作用\n\n`create` 的本质是：\n\n- 先调用 `new()`\n- 并且把对象名、类型等信息带进去\n- 适合 UVM 的工厂模式和对象复制/克隆机制\n\n它的常见形式：\n\n```systemverilog\nobj = my_obj::type_id::create(\"obj\");\n```\n\n其中：\n\n- `type_id` 是对象类型注册信息\n- `create` 会创建一个对象实例\n- 字符串参数 `\"obj\"` 通常是对象名\n\n### 3. 为什么 UVM 里一定要用 `create`\n\nUVM 设计时考虑了“对象创建和类型扩展”的问题。比如：\n\n```systemverilog\nclass my_txn extends uvm_sequence_item;\n  `uvm_object_utils(my_txn)\nendclass\n```\n\n如果你直接写 `new()`，编译器只能看到静态类型；\n\n但 UVM 工厂需要知道真正的动态类型，才能正确创建子类对象。\n\n所以 UVM 中最常用的是：\n\n```systemverilog\nmy_txn txn;\ntxn = my_txn::type_id::create(\"txn\");\n```\n\n这使得对象创建更符合面向对象框架的要求。\n\n### 4. 一个简单例子\n\n```systemverilog\nclass base extends uvm_object;\n  `uvm_object_utils(base)\n\n  function new(string name = \"base\");\n    super.new(name);\n  endfunction\nendclass\n\nclass child extends base;\n  `uvm_object_utils(child)\n\n  function new(string name = \"child\");\n    super.new(name);\n  endfunction\nendclass\n```\n\n创建：\n\n```systemverilog\nbase b;\nchild c;\n\nb = base::type_id::create(\"b\");\nc = child::type_id::create(\"c\");\n```\n\n这样创建的对象都能正确记录自己的类型信息，后续工厂和复制操作会更稳定。\n\n### 5. `create` 和 `new` 的关系\n\n可以把它理解为：\n\n- `new()` 是底层构造函数\n- `create()` 是 UVM 的“规范化创建入口”\n\n`create()` 内部依旧会调用 `new()`，但它增加了 UVM 运行时类型机制。\n\n### 6. 典型 UVM 场景\n\n在 UVM 中，通常会这样创建对象：\n\n```systemverilog\nclass my_agent extends uvm_agent;\n  my_driver drv;\n\n  function void build_phase(uvm_phase phase);\n    super.build_phase(phase);\n    drv = my_driver::type_id::create(\"drv\", this);\n  endfunction\nendclass\n```\n\n这里：\n\n- `create(\"drv\", this)` 说明对象名和父对象\n- 这也是 UVM 里最典型的创建方式\n\n### 7. 一句话总结\n\n`create` 是 UVM 里更标准的对象创建方法，它在 `new()` 的基础上加入了对象类型信息和工厂支持，使得继承、扩展和动态创建更容易管理。\n"
    },
    {
      "kind": 2,
      "language": "systemverilog",
      "value": "## registry 和 factory mechanism（SystemVerilog / UVM）\n\n在 UVM 里，`registry` 和 `factory mechanism` 是一对非常重要的概念。\n\n它们的核心思想是：\n\n- 代码里写的是“抽象类型”\n- 运行时由工厂/注册表决定真正创建哪一个具体类\n- 这样就能在不改代码结构的前提下切换不同实现\n\n### 1. 什么是 registry\n\n`registry` 就是“注册表”，用于把类名和类型信息登记进去。\n\n在 UVM 里，最常见的是：\n\n- `type_id`\n- `uvm_object_registry`\n- `uvm_component_registry`\n\n例如：\n\n```systemverilog\nclass my_txn extends uvm_object;\n  `uvm_object_utils(my_txn)\n\n  function new(string name = \"my_txn\");\n    super.new(name);\n  endfunction\nendclass\n```\n\n这里的宏：\n\n- `uvm_object_utils(my_txn)`\n\n会让这个类在 UVM 中注册自己的类型信息。\n\n之后就可以通过：\n\n```systemverilog\nmy_txn txn;\ntxn = my_txn::type_id::create(\"txn\");\n```\n\n这里 `type_id` 就是注册表提供的类型入口。\n\n### 2. registry 的作用\n\n它的作用就是：\n\n- 让编译器知道这个类是什么类型\n- 让工厂知道它可以创建哪种对象\n- 让 `create()` 能找到对应的构造函数\n\n所以，`registry` 本质上就是“类型登记处”。\n\n### 3. 什么是 mechanism\n\n`mechanism` 一般指“机制、方式、流程”。\n\n在 UVM 里，`factory mechanism` 就是：\n\n- 类注册到工厂\n- 通过工厂创建对象\n- 工厂可以在运行时替换具体实现\n\n这就是一个典型的创建机制。\n\n### 4. 工厂机制的例子\n\n```systemverilog\nclass base_driver extends uvm_driver #(my_item);\n  `uvm_component_utils(base_driver)\n\n  function new(string name = \"base_driver\", uvm_component parent = null);\n    super.new(name, parent);\n  endfunction\nendclass\n\nclass special_driver extends base_driver;\n  `uvm_component_utils(special_driver)\n\n  function new(string name = \"special_driver\", uvm_component parent = null);\n    super.new(name, parent);\n  endfunction\nendclass\n```\n\n然后在 test 里配置：\n\n```systemverilog\nfactory.set_type_override_by_type(base_driver::get_type(),\n                                  special_driver::get_type());\n```\n\n这样以后，原来创建 `base_driver` 的地方，实际可能变成 `special_driver`。\n\n### 5. 真实创建时的调用\n\n```systemverilog\nbase_driver drv;\ndrv = base_driver::type_id::create(\"drv\", this);\n```\n\n这里虽然代码里写的是 `base_driver`，但工厂机制允许它在运行时替换成 `special_driver`。\n\n### 6. registry 和 mechanism 的关系\n\n可以把它理解成：\n\n- `registry`：负责“注册类型”\n- `factory mechanism`：负责“根据注册信息创建对象并替换实现”\n\n也就是说：\n\n- 没有 registry，就不知道有哪些类型可创建\n- 没有 mechanism，就无法在运行时替换不同实现\n\n### 7. 为什么 UVM 要这样设计\n\n因为验证环境中，很多时候我们希望：\n\n- 代码结构固定\n- 但测试场景不同\n- 需要切换不同的 driver / monitor / checker / sequence\n\n如果每处都直接写 `new()`，那么代码会强耦合，修改非常麻烦。\n\n工厂 + registry 让它变成：\n\n- 统一入口\n- 灵活替换\n- 复用性更强\n\n### 8. 一句话总结\n\n`registry` 是“类型注册表”，负责登记类信息；`factory mechanism` 是“运行时对象创建机制”，负责根据注册信息创建对象并支持重写替换。\n\n在 UVM 里，它们结合起来，就是实现可扩展、可替换、可复用验证环境的核心机制。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## override 和工厂下的 proxy class\n\n在 UVM 里，`override` 和 `proxy class` 都是为了让“代码固定、行为可切换”这一思想更容易实现。\n\n### 1. 什么是 override\n\n`override` 的意思就是：\n\n- 父类定义了一个接口或行为\n- 子类把它重新实现\n- 通过工厂/对象创建时，实际使用的是子类版本\n\n这是面向对象中“多态”的体现。\n\n```systemverilog\nclass base_agent;\n  virtual function void run();\n    $display(\"base run\");\n  endfunction\nendclass\n\nclass special_agent extends base_agent;\n  virtual function void run();\n    $display(\"special run\");\n  endfunction\nendclass\n```\n\n调用方式：\n\n```systemverilog\nbase_agent a;\nspecial_agent s = new();\n\na = s;\na.run();\n```\n\n这里虽然句柄类型是 `base_agent`，但真正运行的是 `special_agent::run()`。\n\n### 2. 在 UVM 中的 override\n\nUVM 的工厂重写更像是：\n\n- 你写了一个默认类 `base_driver`\n- 但测试时想替换成 `special_driver`\n- 通过 `factory.set_type_override_by_type()` 来完成\n\n```systemverilog\nclass base_driver extends uvm_driver #(my_item);\n  `uvm_component_utils(base_driver)\nendclass\n\nclass special_driver extends base_driver;\n  `uvm_component_utils(special_driver)\nendclass\n```\n\n配置：\n\n```systemverilog\nfactory.set_type_override_by_type(base_driver::get_type(),\n                                  special_driver::get_type());\n```\n\n这样一来，所有创建 `base_driver` 的地方，实际都可能变成 `special_driver`。\n\n这是 UVM 工厂中的典型 override。\n\n### 3. 为什么叫 override\n\n因为它不是简单地复制一个类，而是在运行时“覆盖默认行为”。\n\n换句话说：\n\n- 默认行为是 base 类\n- override 后，实际行为变成子类\n- 调用代码不需要手工改很多地方\n\n### 4. 什么是工厂下的 proxy class\n\n在工厂机制下面，`proxy class` 常常是指这样一种“替身类”：\n\n- 外部代码只认识基类接口\n- 真正的实现通过工厂替换为某个具体子类\n- 这个中间类像代理一样，把访问转发到真正的对象\n\n比如：\n\n```systemverilog\nclass base_if;\n  virtual function void send();\n    $display(\"base send\");\n  endfunction\nendclass\n\nclass real_if extends base_if;\n  virtual function void send();\n    $display(\"real send\");\n  endfunction\nendclass\n\nclass proxy_if;\n  base_if target;\n\n  function new();\n    target = new();\n  endfunction\n\n  function void send();\n    target.send();\n  endfunction\nendclass\n```\n\n在工厂场景下，`target` 这个对象可能在运行时被替换成 `real_if` 或某个子类对象。\n\n这时 `proxy_if` 就像一个代理层：\n\n- 对外仍然提供统一接口\n- 真正执行时由工厂决定具体对象\n\n### 5. 工厂 + proxy 的价值\n\n它的价值在于：\n\n- 外部代码不关心具体实现\n- 工厂决定真正创建什么对象\n- 代理类负责统一封装和转发\n\n这样就能把“功能实现”和“调用逻辑”分开，方便扩展和替换。\n\n### 6. 一个更贴近 UVM 的理解\n\n在 UVM 中：\n\n- `uvm_agent` 可以看作一个协调层/代理层\n- 它内部持有 driver、monitor、sequencer\n- 工厂可以在测试中替换其中某个组件\n\n这就非常像“代理 + 工厂”的组合：\n\n- 统一接口\n- 中间层管理\n- 具体实现可替换\n\n### 7. 一句话总结\n\n`override` 是“重写默认行为”，而工厂下的 `proxy class` 是“通过中间层统一访问、最终由工厂决定真正执行哪个具体实现”。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## UVM Phase（阶段机制）\n\nUVM phase 是 UVM 按照固定顺序自动执行的一组阶段。它把验证环境的创建、连接、运行和结果检查分开管理。\n\n### 1. 为什么需要 phase\n\n如果没有 phase，所有代码都写在一起，组件之间很难协调执行顺序。\n\nUVM 通过 phase 规定：\n\n- 什么时候创建组件\n- 什么时候连接端口\n- 什么时候开始仿真\n- 什么时候运行激励\n- 什么时候检查结果\n- 什么时候输出报告\n\n### 2. 常用 phase 的执行顺序\n\n```text\nbuild_phase\n    |\nconnect_phase\n    |\nend_of_elaboration_phase\n    |\nstart_of_simulation_phase\n    |\nrun_phase\n    |\nextract_phase\n    |\ncheck_phase\n    |\nreport_phase\n    |\nfinal_phase\n```\n\n### 3. build_phase\n\n`build_phase` 用来创建组件和配置对象，是构建 UVM 验证环境的主要阶段。\n\n```systemverilog\nfunction void build_phase(uvm_phase phase);\n  super.build_phase(phase);\n\n  drv = my_driver::type_id::create(\"drv\", this);\n  mon = my_monitor::type_id::create(\"mon\", this);\nendfunction\n```\n\n常见操作：\n\n- 使用 `type_id::create()` 创建组件\n- 获取配置参数\n- 创建 agent、driver、monitor、sequencer\n\n### 4. connect_phase\n\n`connect_phase` 用来连接 TLM 端口和导出端口。\n\n```systemverilog\nfunction void connect_phase(uvm_phase phase);\n  super.connect_phase(phase);\n\n  mon.ap.connect(scoreboard.analysis_export);\n  drv.seq_item_port.connect(sequencer.seq_item_export);\nendfunction\n```\n\n这个阶段通常不再创建组件，主要负责建立组件之间的数据通路。\n\n### 5. end_of_elaboration_phase\n\n这个阶段发生在环境构建和连接完成之后，常用于检查和打印组件层次结构。\n\n```systemverilog\nfunction void end_of_elaboration_phase(uvm_phase phase);\n  super.end_of_elaboration_phase(phase);\n  uvm_top.print_topology();\nendfunction\n```\n\n### 6. start_of_simulation_phase\n\n这个阶段在仿真正式运行前执行，常用于输出仿真开始信息或初始化统计变量。\n\n```systemverilog\nfunction void start_of_simulation_phase(uvm_phase phase);\n  super.start_of_simulation_phase(phase);\n  `uvm_info(\"SIM\", \"Simulation starts\", UVM_LOW)\nendfunction\n```\n\n### 7. run_phase\n\n`run_phase` 是真正执行时间消耗型验证行为的阶段，例如发送激励、等待时钟和采集数据。\n\n```systemverilog\ntask run_phase(uvm_phase phase);\n  my_sequence seq;\n\n  phase.raise_objection(this);\n\n  seq = my_sequence::type_id::create(\"seq\");\n  seq.start(env.agent.sequencer);\n\n  phase.drop_objection(this);\nendtask\n```\n\n注意：\n\n- `run_phase` 通常使用 `task`\n- 因为里面可能有 `#delay`、`@(posedge clk)` 或其他耗时操作\n- `raise_objection()` 表示暂时不能结束仿真\n- `drop_objection()` 表示该组件的主要运行任务已经完成\n\n### 8. extract_phase\n\n`extract_phase` 用于从组件中提取最终统计数据。\n\n例如：从 scoreboard 或 coverage 组件中提取计数结果。\n\n```systemverilog\nfunction void extract_phase(uvm_phase phase);\n  super.extract_phase(phase);\n  total_count = scoreboard.total_count;\nendfunction\n```\n\n### 9. check_phase\n\n`check_phase` 用于检查验证结果是否正确。\n\n```systemverilog\nfunction void check_phase(uvm_phase phase);\n  super.check_phase(phase);\n\n  if (error_count == 0)\n    `uvm_info(\"CHECK\", \"Test passed\", UVM_LOW)\n  else\n    `uvm_error(\"CHECK\", \"Test failed\")\nendfunction\n```\n\n### 10. report_phase\n\n`report_phase` 用于输出最终测试报告。\n\n```systemverilog\nfunction void report_phase(uvm_phase phase);\n  super.report_phase(phase);\n  `uvm_info(\"REPORT\", \"Verification completed\", UVM_LOW)\nendfunction\n```\n\n### 11. function phase 和 task phase 的区别\n\n| 类型 | 特点 | 常见 phase |\n| --- | --- | --- |\n| `function` phase | 不能消耗仿真时间 | `build_phase`、`connect_phase`、`report_phase` |\n| `task` phase | 可以等待事件和消耗仿真时间 | `run_phase` |\n\n例如：\n\n```systemverilog\nfunction void build_phase(uvm_phase phase);\n  // 不能写 #10 或 @(posedge clk)\nendfunction\n\ntask run_phase(uvm_phase phase);\n  // 可以写延时和事件控制\n  #10;\n  @(posedge clk);\nendtask\n```\n\n### 12. 一句话总结\n\nUVM phase 就是 UVM 规定好的生命周期流程：先 `build` 创建组件，再 `connect` 连接组件，然后在 `run` 中执行验证，最后通过 `check` 和 `report` 判断并输出结果。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 打印 UVM 层次结构：`print_topology()`\n\n在 UVM 中，组件按照父子关系组成一棵层次树。为了检查组件是否正确创建、命名和连接，可以使用：\n\n```systemverilog\nuvm_top.print_topology();\n```\n\n### 1. 常见调用位置\n\n通常在 `end_of_elaboration_phase` 中打印，因为这个阶段已经完成了组件创建和连接。\n\n```systemverilog\nfunction void end_of_elaboration_phase(uvm_phase phase);\n  super.end_of_elaboration_phase(phase);\n\n  uvm_top.print_topology();\nendfunction\n```\n\n也可以通过当前组件句柄调用：\n\n```systemverilog\nthis.print();\n```\n\n不过，想查看完整的 UVM 环境层次时，最常用的是：\n\n```systemverilog\nuvm_top.print_topology();\n```\n\n### 2. 一个简单的层次结构\n\n假设环境中包含：\n\n```text\nuvm_test_top\n`-- env\n    `-- agent\n        |-- sequencer\n        |-- driver\n        `-- monitor\n```\n\n调用 `print_topology()` 后，日志中会打印类似的层次信息：\n\n```text\nUVM_INFO ... uvm_test_top.env.agent.driver\nUVM_INFO ... uvm_test_top.env.agent.monitor\nUVM_INFO ... uvm_test_top.env.agent.sequencer\n```\n\n实际输出格式会根据 UVM 版本和 simulator 略有不同。\n\n### 3. 它能检查什么\n\n`print_topology()` 主要用于检查：\n\n- 组件是否被正确创建\n- 组件名称是否正确\n- 父子层次关系是否正确\n- driver、monitor、sequencer 是否存在\n- 是否出现组件没有创建或创建位置错误的问题\n\n### 4. 和 `print()` 的区别\n\n```systemverilog\nuvm_top.print_topology();\n```\n\n主要打印整个 UVM 组件树，适合查看组件层次。\n\n```systemverilog\nobject.print();\n```\n\n主要打印某一个对象的字段内容，适合查看 transaction 或配置对象的数据。\n\n例如：\n\n```systemverilog\nmy_item item;\nitem = my_item::type_id::create(\"item\");\nitem.print();\n```\n\n所以：\n\n- `print_topology()`：看组件层次\n- `print()`：看对象成员数据\n\n### 5. 常见问题\n\n如果打印结果中没有看到某个组件，通常检查：\n\n- 是否在 `build_phase` 中调用了 `type_id::create()`\n- 创建时的 parent 是否正确\n- 类是否使用了正确的注册宏\n- `build_phase` 中是否调用了 `super.build_phase(phase)`\n\n### 一句话总结\n\n`uvm_top.print_topology()` 用来打印整个 UVM 验证环境的组件层次，是检查组件创建和层次关系最常用的调试方法。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## 哪些类有 UVM phase\n\n通常来说，只有继承自 `uvm_component` 的类才会参与 UVM 的 component phase 机制。\n\n### 1. `uvm_component` 有 phase\n\n常见的组件包括：\n\n```systemverilog\nclass my_env extends uvm_env;\nclass my_agent extends uvm_agent;\nclass my_driver extends uvm_driver #(my_item);\nclass my_monitor extends uvm_monitor;\nclass my_sequencer extends uvm_sequencer #(my_item);\nclass my_scoreboard extends uvm_scoreboard;\n```\n\n这些类都继承自 `uvm_component`，因此可以实现：\n\n```systemverilog\nfunction void build_phase(uvm_phase phase);\nendfunction\n\ntask run_phase(uvm_phase phase);\nendtask\n```\n\nUVM 会按照组件层次自动调用这些 phase 方法。\n\n### 2. `uvm_object` 通常没有 phase\n\n以下对象一般继承自 `uvm_object`：\n\n```systemverilog\nclass my_item extends uvm_sequence_item;\nclass my_config extends uvm_object;\nclass my_sequence extends uvm_sequence #(my_item);\n```\n\n它们主要用于：\n\n- 保存 transaction 数据\n- 保存配置参数\n- 产生和发送激励\n- 进行对象复制、比较和打印\n\n它们本身不属于 UVM 组件树，因此通常没有自己的 `build_phase()` 或 `run_phase()`。\n\n### 3. sequence 如何参与运行\n\nSequence 本身不是 component，所以它没有独立的 component phase。但是它可以被 component 的 `run_phase` 启动：\n\n```systemverilog\ntask run_phase(uvm_phase phase);\n  my_sequence seq;\n\n  phase.raise_objection(this);\n\n  seq = my_sequence::type_id::create(\"seq\");\n  seq.start(env.agent.sequencer);\n\n  phase.drop_objection(this);\nendtask\n```\n\n这里：\n\n- `run_phase` 属于 component\n- `my_sequence` 属于 `uvm_object` 体系\n- sequence 通过 `start()` 在 sequencer 上运行\n- sequence 的执行发生在 component 的运行阶段中\n\n### 4. 继承关系对比\n\n```text\nuvm_object\n    |\n    +-- uvm_transaction\n            |\n            +-- uvm_sequence_item\n                    |\n                    +-- uvm_sequence\n\nuvm_object\n    |\n    +-- uvm_component\n            |\n            +-- uvm_driver / uvm_monitor / uvm_agent / uvm_env\n```\n\n重点是：\n\n- `uvm_component`：有层次结构，有 phase\n- `uvm_object`：没有组件层次，通常没有 phase\n\n### 5. 一个容易混淆的地方\n\n`uvm_phase` 本身也是一个用于描述阶段的对象类型，但这不代表所有 `uvm_object` 都拥有 phase 回调。\n\n“有一个 `uvm_phase` 参数”和“属于 UVM component phase 体系”是两件不同的事。\n\n### 一句话总结\n\n严格地说，只有 `uvm_component` 及其子类拥有并参与 UVM 的 component phase；`uvm_object` 及其子类没有独立 phase，但可以在 component 的 phase 中被创建、启动或使用。\n"
    },
    {
      "kind": 1,
      "language": "markdown",
      "value": "## `::`（作用域解析）和 `.`（成员访问）的区别\n\n### 一句话总结\n\n> **`::` 走\"类的大门\"，`.` 走\"对象的大门\"。**\n\n---\n\n### 核心概念\n\n| 概念 | 是什么 | 比喻 |\n|------|--------|------|\n| 类 `class` | 模板/蓝图 | 手机的设计图纸 |\n| 对象 `new()` | 按图纸造出来的实例 | 你手里那台真实的手机 |\n| 变量 | 存数据的空间 | 手机里的\"存储空间\" |\n\n一张图纸（类）可以造出无数个实物（对象），每个对象有自己的变量。\n\n---\n\n### `::` 的用法\n\n`::` 是**作用域解析运算符**，在**类/类型层面**操作，**不需要 `new` 实例化对象**。\n\n凡是\"属于类这个家族\"的东西，都用 `::` 访问：\n\n#### 1. 拿嵌套类型\n\n```systemverilog\nclass counter;\n  typedef int count_t;\n  class result;\n    int pass, fail;\n  endclass\nendclass\n\ncounter::count_t  a;\ncounter::result   r;\n```\n\n#### 2. 拿静态变量\n\n```systemverilog\nclass counter;\n  static int total = 0;\nendclass\n\ncounter::total = 100;\n```\n\n#### 3. 拿参数/常量\n\n```systemverilog\nclass counter;\n  parameter int MAX = 255;\n  localparam int MIN = 0;\nendclass\n\n$display(\"%0d\", counter::MAX);\n```\n\n#### 4. 调用静态方法\n\n```systemverilog\nclass counter;\n  static function int get_version();\n    return 1;\n  endfunction\nendclass\n\n$display(\"%0d\", counter::get_version());\n```\n\n#### 5. 在类外部定义方法\n\n```systemverilog\nclass counter;\n  int count;\n  function void print();\nendclass\n\nfunction void counter::print();\n  $display(\"count = %0d\", count);\nendfunction\n```\n\n---\n\n### `.` 的用法\n\n`.` 是**成员访问运算符**，在**对象/实例层面**操作，**必须先 `new()` 创建实例**。\n\n```systemverilog\ncounter c1 = new(5);\nc1.val = 10;\nc1.start();\n```\n\n---\n\n### 对比总表\n\n| 场景 | 用哪个 | 要不要 new | 例子 |\n|------|--------|:---:|------|\n| 访问类的嵌套类型 | `::` | ❌ | `counter::count_t` |\n| 访问类的静态成员 | `::` | ❌ | `counter::total` |\n| 访问类的 parameter | `::` | ❌ | `counter::MAX` |\n| 调用静态方法 | `::` | ❌ | `counter::get_version()` |\n| 类外定义方法 | `::` | ❌ | `counter::print()` |\n| 创建对象 | `new` | — | `counter c1 = new()` |\n| 访问对象的成员变量 | `.` | ✅ | `c1.val` |\n| 调用对象的方法 | `.` | ✅ | `c1.start()` |\n\n---\n\n### 一个重要认知\n\n**`::` 和 `.` 不是\"二选一\"的关系，而是各管各的，不能互相替代。**\n\n| 属于谁 | 用什么 | 要不要 new |\n|--------|--------|:---:|\n| 属于**类本身**（静态变量、嵌套类型、参数） | `::` | ❌ 不需要 |\n| 属于**每个对象**（成员变量、成员方法） | `.` | ✅ 必须先 new |\n\n---\n\n### 同一个名字，不同含义\n\n```systemverilog\ncounter::count c1    // :: → count 是嵌套类名（类型），声明变量 c1\nc1.count             // .  → count 是成员变量名（数据）\n```\n\n这两个 `count` 长得像，但完全是不同的东西：\n- 第一个 `count` 是**嵌套类名**（类型）\n- 第二个 `count` 是**成员变量名**（数据）\n\n---\n\n### 静态变量的特殊情况\n\n```systemverilog\nclass counter;\n  static int count = 0;    // 静态变量，只有一份，属于类\nendclass\n\nmodule tb;\n  initial begin\n    counter c1 = new();\n    counter c2 = new();\n\n    $display(\"count = %0d\", counter::count);  // ✅ 推荐写法\n    $display(\"count = %0d\", c1.count);        // ⚠️ 可以但不推荐\n    $display(\"count = %0d\", c2.count);        // ⚠️ 可以但不推荐\n    // 三行打印的是同一个值，因为静态变量只有一份\n  end\nendmodule\n```\n\n如果 `count` 是普通成员变量（非 static），`counter::count` 会直接编译报错。\n\n---\n\n### 快速记忆\n\n> 记住：**`类名::xxx` 就是\"从类这个抽屉里拿 xxx\"，跟有没有 new 对象没关系。**\n>\n> **`对象.xxx` 就是\"从这个对象身上拿 xxx\"，必须先 new 出对象。**"
    }
  ]
}